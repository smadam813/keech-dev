---
phase: 02-content-blog
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/blog/mdx-content.tsx
  - src/components/blog/post-card.tsx
  - src/components/blog/toc.tsx
  - src/components/blog/tag-chip.tsx
  - src/app/blog/page.tsx
  - src/app/blog/[slug]/page.tsx
  - src/app/globals.css
autonomous: false

must_haves:
  truths:
    - "Blog listing page shows all posts with titles, dates, excerpts, and tags"
    - "Clicking a post card navigates to /blog/[slug]"
    - "Individual post page renders MDX content with proper formatting"
    - "Code blocks display with syntax highlighting and line numbers"
    - "Posts show reading time and publication date"
    - "Table of contents sidebar is visible on desktop"
    - "Tags are styled as neobrutalist chips"
  artifacts:
    - path: "src/app/blog/page.tsx"
      provides: "Blog listing page"
      contains: "posts.filter"
    - path: "src/app/blog/[slug]/page.tsx"
      provides: "Individual post page with TOC"
      contains: "generateStaticParams"
    - path: "src/components/blog/mdx-content.tsx"
      provides: "Client-side MDX renderer"
      contains: "use client"
    - path: "src/components/blog/toc.tsx"
      provides: "Table of contents component"
      contains: "sticky"
    - path: "src/components/blog/post-card.tsx"
      provides: "Post card for listing"
      contains: "Link"
    - path: "src/components/blog/tag-chip.tsx"
      provides: "Neobrutalist tag chip"
      contains: "border-brutal"
  key_links:
    - from: "src/app/blog/page.tsx"
      to: "@/.velite"
      via: "import posts"
      pattern: "import.*posts.*@/\\.velite"
    - from: "src/app/blog/[slug]/page.tsx"
      to: "src/components/blog/mdx-content.tsx"
      via: "MDXContent component"
      pattern: "MDXContent.*code.*post\\.body"
    - from: "src/app/blog/[slug]/page.tsx"
      to: "src/components/blog/toc.tsx"
      via: "TableOfContents with post.toc"
      pattern: "TableOfContents.*entries.*post\\.toc"
---

<objective>
Build blog UI components and pages

Purpose: Transform the Velite content engine into a user-facing blog. Visitors will browse posts on the listing page and read individual articles with comfortable typography, syntax highlighting, and table of contents navigation.

Output: Complete blog functionality - listing page, post pages, and all supporting components.
</objective>

<execution_context>
@/home/smada/.claude/get-shit-done/workflows/execute-plan.md
@/home/smada/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-blog/02-RESEARCH.md
@.planning/phases/02-content-blog/02-CONTEXT.md
@.planning/phases/02-content-blog/02-01-SUMMARY.md
@src/app/globals.css
@src/lib/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create blog UI components</name>
  <files>
    src/components/blog/mdx-content.tsx
    src/components/blog/post-card.tsx
    src/components/blog/toc.tsx
    src/components/blog/tag-chip.tsx
  </files>
  <action>
1. Create src/components/blog/ directory

2. Create mdx-content.tsx (client component):
   - 'use client' directive required
   - Import runtime from 'react/jsx-runtime'
   - useMDXComponent hook that creates component from Velite's compiled code
   - MDXContent component accepting code string and optional components
   - Export MDXContent as named export

3. Create post-card.tsx:
   - Accept post prop with type from Velite (title, slug, date, description, excerpt, tags, readingTime)
   - Link wrapping the entire card (href to /blog/{slug})
   - Neobrutalist card styling: border-[3px] border-black, shadow-brutal, bg-surface
   - Hover effect: shadow-brutal-hover, slight translate
   - Display: title (h2), date formatted nicely, excerpt (or description), reading time
   - Tags rendered using TagChip component
   - Responsive: stack on mobile, comfortable padding

4. Create toc.tsx:
   - TableOfContents component accepting entries array (TocEntry type with title, url, items)
   - Sticky positioning: sticky top-24 max-h-[calc(100vh-8rem)] overflow-auto
   - "Contents" heading with font-display
   - Recursive TocList for nested headings
   - Links with text-muted hover:text-foreground transition
   - Indent nested items with ml-4

5. Create tag-chip.tsx:
   - Accept tag string, optional href, optional className
   - If href provided, render as Link; otherwise span
   - Neobrutalist chip: px-3 py-1 text-sm font-bold border-[3px] border-black bg-surface
   - Shadow-brutal-hover on hover, transition-shadow
   - Use cn() utility for className merging
  </action>
  <verify>
    Run `npm run lint` - no errors
    Run `npm run build` - no TypeScript errors
  </verify>
  <done>
    All four blog components exist with proper TypeScript types and neobrutalist styling
  </done>
</task>

<task type="auto">
  <name>Task 2: Build blog listing and post pages</name>
  <files>
    src/app/blog/page.tsx
    src/app/blog/[slug]/page.tsx
    src/app/globals.css
  </files>
  <action>
1. Replace src/app/blog/page.tsx placeholder:
   - Import posts from '@/.velite'
   - Filter out drafts (post.draft !== true)
   - Sort by date descending (newest first)
   - Page header: "Blog" title with font-display, brief intro text
   - Grid layout: gap-6, 1 col mobile, 2 cols md, 3 cols lg
   - Map posts to PostCard components
   - Handle empty state gracefully

2. Create src/app/blog/[slug]/page.tsx:
   - Import posts from '@/.velite', notFound from 'next/navigation'
   - generateStaticParams: return posts.map(post => ({ slug: post.slug }))
   - Page component: async, receives params Promise
   - Find post by slug, call notFound() if not found
   - Layout: container mx-auto, grid with main content + TOC sidebar
   - Main content (left): prose container with max-width 65ch
   - Header: post.title (h1), date formatted, reading time, tags
   - MDXContent with post.body
   - Sidebar (right, hidden on mobile): TableOfContents with post.toc
   - Show "last updated" if post.updated exists
   - Add generateMetadata for SEO (title, description)

3. Add prose styles to src/app/globals.css:
   - .prose class with max-width: 65ch, font-size: 1.125rem (18px), line-height: 1.7
   - Heading styles: h2 mt-12 mb-4 text-2xl, h3 mt-8 mb-3 text-xl
   - Paragraph: mb-6
   - Links: text-accent underline hover:text-accent-hover
   - Lists: mb-6 pl-6, li mb-2
   - Blockquote: border-l-4 border-accent pl-4 italic my-6
   - Mobile responsive: smaller font-size on small screens
  </action>
  <verify>
    Run `npm run dev` and navigate to:
    - /blog - should show post card(s)
    - /blog/hello-world - should show full post with TOC
    Check code blocks render with syntax highlighting and line numbers
  </verify>
  <done>
    Blog listing shows posts, individual posts render with MDX, TOC, and syntax highlighting
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify blog experience</name>
  <what-built>Complete blog system with listing and post pages</what-built>
  <how-to-verify>
1. Open http://localhost:3000/blog
   - Should see "Blog" heading
   - Post card for "Hello World" (or your sample post title) should be visible
   - Card should show title, date, excerpt, reading time, tags

2. Click the post card
   - Should navigate to /blog/hello-world
   - Post title and metadata should appear at top
   - Table of contents should be visible on right sidebar (desktop only)
   - MDX content should render with proper typography
   - Code block should have:
     - Syntax highlighting (colored keywords)
     - Line numbers
     - Language badge showing "typescript"
     - Copy button on hover

3. Check responsive behavior
   - On mobile: TOC sidebar should be hidden
   - Post content should be readable on mobile
   - Card grid should stack to single column

4. Navigate back to /blog
   - Header navigation should work
  </how-to-verify>
  <resume-signal>Type "approved" if blog works correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:

1. Blog listing at /blog shows all non-draft posts
2. Posts sorted by date (newest first)
3. Post cards display title, date, excerpt, reading time, tags
4. Individual post pages render MDX with proper typography
5. Code blocks have syntax highlighting with line numbers
6. Table of contents visible on desktop, hidden on mobile
7. All typography follows the 65ch optimal reading width
8. Tags styled as neobrutalist chips
9. Navigation between listing and posts works
</verification>

<success_criteria>
- Visitors can browse blog posts and see titles, dates, excerpts
- Clicking a post navigates to the full article
- Articles render MDX with proper formatting
- Code blocks display with syntax highlighting and line numbers
- Reading time and date visible on posts
- Table of contents aids navigation on longer posts
- Typography is comfortable for long-form reading
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-blog/02-02-SUMMARY.md`
</output>

---
phase: quick
plan: 260403-h8h
type: execute
wave: 1
depends_on: []
files_modified: [e2e/code-copy.spec.ts]
autonomous: true
requirements: [QUICK-h8h]

must_haves:
  truths:
    - "code-copy spec no longer references any hardcoded slug"
    - "code-copy spec navigates via /blog listing like the other 3 specs"
    - "code-copy spec still skips gracefully when no code blocks found"
  artifacts:
    - path: "e2e/code-copy.spec.ts"
      provides: "Dynamic blog post navigation for code copy testing"
      contains: "a[href^=\"/blog/\"]"
  key_links:
    - from: "e2e/code-copy.spec.ts"
      to: "/blog listing page"
      via: "page.goto('/blog') then click first post link"
      pattern: "goto.*blog.*locator.*blog"
---

<objective>
Replace the hardcoded slug in `e2e/code-copy.spec.ts` with dynamic navigation via the blog listing page, matching the pattern already used by the other three E2E specs.

Purpose: Eliminate brittle test that breaks if post is renamed, deleted, or set to draft.
Output: Updated `e2e/code-copy.spec.ts` with dynamic post lookup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@e2e/code-copy.spec.ts
@e2e/view-count.spec.ts (reference pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded slug with dynamic listing navigation</name>
  <files>e2e/code-copy.spec.ts</files>
  <action>
Replace line 8 (`await page.goto('/blog/jira-vs-markdown-ai-agents')`) with the navigate-via-listing pattern used by view-count.spec.ts and mobile-toc.spec.ts:

```typescript
await page.goto('/blog')
const firstPost = page.locator('a[href^="/blog/"]').first()
await firstPost.click()
await page.waitForURL(/\/blog\/.+/)
```

Update the comment on line 7 from "Navigate directly to a blog post known to have code blocks" to "Navigate to first blog post via listing page" (or similar). The existing skip guard on lines 14-17 already handles the case where the post has no code blocks, so no other changes needed.

Do NOT implement Option B (iterating posts) from the research -- Option A is sufficient per research recommendation.
  </action>
  <verify>
    <automated>grep -c "jira-vs-markdown" e2e/code-copy.spec.ts | grep -q "^0$" && grep -c 'a\[href\^="/blog/"\]' e2e/code-copy.spec.ts | grep -q "^1$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>No hardcoded slug in code-copy.spec.ts. Navigation uses dynamic listing pattern matching the other 3 specs.</done>
</task>

</tasks>

<verification>
- `grep -r "jira-vs-markdown" e2e/` returns no results
- `e2e/code-copy.spec.ts` contains `page.goto('/blog')` and `a[href^="/blog/"]` locator
- All 4 E2E specs now use dynamic navigation (no hardcoded slugs anywhere in `e2e/`)
</verification>

<success_criteria>
- code-copy.spec.ts navigates via /blog listing, not a hardcoded slug
- Existing test logic (hover, copy, copied state, revert) unchanged
- Skip guard still present for posts without code blocks
</success_criteria>

<output>
After completion, create `.planning/quick/260403-h8h-hardcoded-slug-in-e2e-spec-swap-for-a-dy/260403-h8h-SUMMARY.md`
</output>

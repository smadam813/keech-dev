---
phase: 08-hero-image
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Hero image fills the viewport without awkward cropping on mobile portrait"
    - "Hero image fills the viewport without awkward cropping on desktop widescreen"
    - "keech.dev text is clearly readable and prominent against the hero background"
    - "The .dev teal accent is visually distinct and recognizable"
    - "The overall impression is atmospheric and distinctive, not generic"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of the hero implementation across responsive breakpoints. Confirm the hero image, scrim, text contrast, and responsive scaling all look correct on real viewport sizes.

Purpose: Catch visual issues that automated checks cannot detect — cropping problems, scrim opacity feel, text prominence, overall atmospheric impression.
Output: User approval or list of visual adjustments needed.
</objective>

<execution_context>
@/home/smada/.claude/get-shit-done/workflows/execute-plan.md
@/home/smada/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-hero-image/08-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full-viewport hero section on the home page with Norse landscape background image, dark radial gradient scrim overlay, and centered "keech.dev" text in white with lighter teal .dev accent (#4FBFBF).
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`
    2. Open `http://localhost:3000` in your browser

    **Desktop check (1440px+ width):**
    - Hero fills the entire viewport below the header
    - Norse landscape image visible behind semi-transparent dark overlay
    - "keech.dev" is dead center, large (128px at lg), white text with teal ".dev"
    - Text is the dominant visual — first thing you notice
    - Overall mood feels atmospheric and distinctive

    **Tablet check (768px width):**
    - Hero still fills viewport
    - Text scales down appropriately (~96px at md)
    - Image cropping looks natural

    **Mobile check (375px width):**
    - Hero fills viewport without requiring scroll to see full image
    - Text is still readable and prominent (~60px at base)
    - No horizontal overflow
    - Address bar doesn't cause layout issues (svh units)

    **Contrast check:**
    - White "keech" text is easily readable against the scrim
    - Teal ".dev" text is clearly visible and recognizably teal
    - Scrim feels natural (vignette effect, not flat overlay)

    **Animation check:**
    - Text fades in on page load (animate-on-load)
    - Blur placeholder shows briefly before image loads (visible on hard refresh with throttled network)
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 8, or describe any visual adjustments needed (e.g., "scrim too dark", "text too small on mobile", "teal needs to be brighter").</resume-signal>
</task>

</tasks>

<verification>
User has visually confirmed:
1. Hero looks correct on desktop, tablet, and mobile viewports
2. Text contrast is sufficient and readable
3. Overall impression meets the "distinctive, not generic" bar
4. No visual issues that need fixing
</verification>

<success_criteria>
- User approves the visual appearance of the hero across responsive breakpoints
- No visual adjustments requested (or adjustments have been applied and re-approved)
</success_criteria>

<output>
After completion, create `.planning/phases/08-hero-image/08-02-SUMMARY.md`
</output>

---
phase: 17-syntax-highlighting-theme-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - velite.config.ts
  - src/app/globals.css
autonomous: false
requirements:
  - SYN-01
  - SYN-02
  - SYN-03
  - SYN-04
must_haves:
  truths:
    - "Code blocks display syntax-highlighted tokens with colors sourced from CSS custom properties"
    - "Token color variables are defined in globals.css Code Block Styles section"
    - "Code block background is #22272e, set via CSS variable (not inline style from Shiki)"
    - "Code blocks look visually similar to current github-dark-dimmed appearance"
  artifacts:
    - path: "velite.config.ts"
      provides: "CSS-variables theme via createCssVariablesTheme(), keepBackground: false"
      contains: "createCssVariablesTheme"
    - path: "src/app/globals.css"
      provides: "Shiki token color CSS variables and pre background"
      contains: "--shiki-foreground"
  key_links:
    - from: "velite.config.ts"
      to: "shiki"
      via: "import { createCssVariablesTheme } from 'shiki'"
      pattern: "createCssVariablesTheme"
    - from: "src/app/globals.css"
      to: "velite.config.ts"
      via: "CSS variables referenced by Shiki's generated var() calls in HTML"
      pattern: "--shiki-token-keyword"
    - from: "src/app/globals.css"
      to: "figure[data-rehype-pretty-code-figure] pre"
      via: "background-color using --shiki-background variable"
      pattern: "background-color.*var.*--shiki-background"
---

<objective>
Migrate syntax highlighting from Shiki's bundled github-dark-dimmed theme to a CSS-variables theme, moving token colors into CSS custom properties in globals.css.

Purpose: Align syntax highlighting with the site's CSS-first design token approach. Token colors become explicit, editable CSS rather than opaque Shiki internals.
Output: Two modified files -- velite.config.ts (theme config) and globals.css (CSS variable definitions)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-syntax-highlighting-theme-migration/17-CONTEXT.md
@.planning/phases/17-syntax-highlighting-theme-migration/17-RESEARCH.md

<interfaces>
<!-- Current velite.config.ts rehype-pretty-code configuration (lines 82-97) -->
From velite.config.ts:
```typescript
markdown: {
  rehypePlugins: [
    rehypeSlug,
    [
      rehypePrettyCode,
      {
        theme: 'github-dark-dimmed',
        keepBackground: true,
        defaultLang: {
          block: 'typescript',
          inline: 'typescript'
        }
      }
    ],
    rehypeListRole
  ]
}
```

<!-- Current globals.css Code Block Styles section starts at line 228 -->
<!-- figure[data-rehype-pretty-code-figure] pre at line 250 has: @apply overflow-x-auto py-4; margin: 0; -->
<!-- No background-color is currently set in CSS -- it comes from Shiki's inline style -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch Velite config to CSS-variables theme</name>
  <files>velite.config.ts</files>
  <read_first>
    - velite.config.ts (current config with theme: 'github-dark-dimmed' and keepBackground: true)
    - .planning/phases/17-syntax-highlighting-theme-migration/17-RESEARCH.md (verified API signatures)
  </read_first>
  <action>
    Modify velite.config.ts to use the CSS-variables theme per D-01, D-02, D-03:

    1. Add import at the top of the file (after existing imports, before rehypeListRole function):
       `import { createCssVariablesTheme } from 'shiki'`

    2. Create the theme instance after the imports, before the collections:
       `const cssVarTheme = createCssVariablesTheme()`
       No arguments needed -- defaults use `--shiki-` prefix per D-02.

    3. In the rehypePrettyCode options object (inside `markdown.rehypePlugins`):
       - Change `theme: 'github-dark-dimmed'` to `theme: cssVarTheme`
       - Change `keepBackground: true` to `keepBackground: false` per D-03
       - Leave `defaultLang` unchanged

    The final rehypePrettyCode options should be:
    ```
    {
      theme: cssVarTheme,
      keepBackground: false,
      defaultLang: {
        block: 'typescript',
        inline: 'typescript'
      }
    }
    ```
  </action>
  <verify>
    <automated>cd /home/smada/git/smadam813/keech-dev && grep -q "createCssVariablesTheme" velite.config.ts && grep -q "keepBackground: false" velite.config.ts && grep -q "theme: cssVarTheme" velite.config.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - velite.config.ts contains `import { createCssVariablesTheme } from 'shiki'`
    - velite.config.ts contains `const cssVarTheme = createCssVariablesTheme()`
    - velite.config.ts contains `theme: cssVarTheme` (not `theme: 'github-dark-dimmed'`)
    - velite.config.ts contains `keepBackground: false` (not `keepBackground: true`)
    - velite.config.ts still contains `defaultLang:` with block/inline typescript
    - velite.config.ts still contains `rehypeSlug` and `rehypeListRole` in the plugins array
    - `npm run velite` completes without errors
  </acceptance_criteria>
  <done>Velite config uses createCssVariablesTheme() with keepBackground: false. Content compiles successfully via npm run velite.</done>
</task>

<task type="auto">
  <name>Task 2: Define Shiki CSS token variables in globals.css</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (current Code Block Styles section starting at line 228)
    - .planning/phases/17-syntax-highlighting-theme-migration/17-RESEARCH.md (color mapping table)
  </read_first>
  <action>
    Add CSS variable definitions to globals.css per D-04, D-05, D-06:

    1. In the Code Block Styles section (after the section comment on line 228, before the existing `figure[data-rehype-pretty-code-figure]` rule on line 235), add a new `:root` block with all Shiki token variables. Place it inside the existing `@layer components` block that starts on line 232:

    ```css
    /* Shiki CSS-variables theme tokens (approximating github-dark-dimmed) */
    :root {
      --shiki-foreground: #adbac7;
      --shiki-background: #22272e;
      --shiki-token-comment: #768390;
      --shiki-token-keyword: #f47067;
      --shiki-token-string: #96d0ff;
      --shiki-token-function: #dcbdfb;
      --shiki-token-constant: #6cb6ff;
      --shiki-token-parameter: #adbac7;
      --shiki-token-string-expression: #8ddb8c;
      --shiki-token-punctuation: #adbac7;
      --shiki-token-link: #539bf5;
    }
    ```

    2. Add `background-color: var(--shiki-background);` to the existing `figure[data-rehype-pretty-code-figure] pre` rule (currently at line 250). The rule currently has `@apply overflow-x-auto py-4;` and `margin: 0;`. Add the background-color line after the existing properties:

    ```css
    figure[data-rehype-pretty-code-figure] pre {
      @apply overflow-x-auto py-4;
      margin: 0;
      background-color: var(--shiki-background);
    }
    ```

    Do NOT place the variables in the `@theme` block (per D-04).
    Do NOT modify the inline code styles (`span[data-rehype-pretty-code-figure] code`) -- the existing `background-color: rgba(0, 0, 0, 0.69) !important` override is sufficient.
  </action>
  <verify>
    <automated>cd /home/smada/git/smadam813/keech-dev && grep -q "\-\-shiki-foreground: #adbac7" src/app/globals.css && grep -q "\-\-shiki-background: #22272e" src/app/globals.css && grep -q "\-\-shiki-token-keyword: #f47067" src/app/globals.css && grep -q "\-\-shiki-token-function: #dcbdfb" src/app/globals.css && grep -q "background-color: var(--shiki-background)" src/app/globals.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - globals.css contains `--shiki-foreground: #adbac7` inside a `:root` block
    - globals.css contains `--shiki-background: #22272e`
    - globals.css contains `--shiki-token-comment: #768390`
    - globals.css contains `--shiki-token-keyword: #f47067`
    - globals.css contains `--shiki-token-string: #96d0ff`
    - globals.css contains `--shiki-token-function: #dcbdfb`
    - globals.css contains `--shiki-token-constant: #6cb6ff`
    - globals.css contains `--shiki-token-parameter: #adbac7`
    - globals.css contains `--shiki-token-string-expression: #8ddb8c`
    - globals.css contains `--shiki-token-punctuation: #adbac7`
    - globals.css contains `--shiki-token-link: #539bf5`
    - globals.css contains `background-color: var(--shiki-background)` in the `figure[data-rehype-pretty-code-figure] pre` rule
    - The `:root` block with shiki variables is NOT inside the `@theme` block
    - The `:root` block is inside the Code Block Styles `@layer components` section
    - `npm run build` completes successfully
  </acceptance_criteria>
  <done>All 11 Shiki CSS token variables defined in globals.css Code Block Styles section. Pre background set via CSS variable. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of code block appearance</name>
  <files>src/app/globals.css</files>
  <action>
    CHECKPOINT: Human verifies visual appearance of code blocks after theme migration (per D-08).

    What was built: Syntax highlighting migrated from bundled github-dark-dimmed theme to CSS-variables theme. Token colors now come from CSS custom properties in globals.css instead of hardcoded Shiki internals. Code block background explicitly set via CSS variable.

    Steps to verify:
    1. Run `npm run dev` to start the development server
    2. Navigate to a blog post with code blocks (e.g., any post with TypeScript/JavaScript examples)
    3. Verify code blocks have a dark background (#22272e) -- not the page's dusty rose background
    4. Verify syntax tokens have distinct colors:
       - Keywords (const, import, function, return) should be red/coral (#f47067)
       - Strings should be light blue (#96d0ff)
       - Comments should be gray (#768390)
       - Function names should be purple/lavender (#dcbdfb)
       - Constants/types should be blue (#6cb6ff)
    5. Check that inline code (backtick code) still has its dark semi-transparent background
    6. Some color differences from before are expected (per D-05/D-08) -- the key check is that code is readable, tokens have distinct colors, and the overall dark appearance is preserved
    7. Verify the copy button on code blocks still works

    Resume signal: Type "approved" if code blocks look acceptable, or describe any visual issues.
  </action>
  <verify>
    <automated>cd /home/smada/git/smadam813/keech-dev && npm run test:e2e -- e2e/code-copy.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Code blocks have dark background (#22272e), not page background
    - Syntax tokens show distinct colors (not all same color)
    - Keywords are red/coral, strings are blue, comments are gray
    - Inline code retains dark semi-transparent background
    - Copy button on code blocks still functions
    - No jarring visual regressions (some granularity loss is acceptable per D-05)
  </acceptance_criteria>
  <done>User confirms code blocks look visually acceptable after theme migration. E2E code-copy test passes.</done>
</task>

</tasks>

<verification>
- `npm run velite` succeeds (content compiles with new theme)
- `npm run build` succeeds (full build with CSS variables)
- `npm run test -- --run` passes (existing unit tests unaffected)
- `npm run test:e2e -- e2e/code-copy.spec.ts` passes (code block copy button still works)
- grep confirms `createCssVariablesTheme` in velite.config.ts
- grep confirms `--shiki-foreground` through `--shiki-token-link` in globals.css
- grep confirms `keepBackground: false` in velite.config.ts
- grep confirms `background-color: var(--shiki-background)` in globals.css
</verification>

<success_criteria>
- Code blocks render with syntax highlighting colors sourced from CSS variables (SYN-01, SYN-02)
- Code block background is explicitly set via CSS, not from Shiki inline style (SYN-03)
- Visual appearance is consistent with github-dark-dimmed character -- dark background, distinct token colors (SYN-04)
- No regressions in existing tests or build
</success_criteria>

<output>
After completion, create `.planning/phases/17-syntax-highlighting-theme-migration/17-01-SUMMARY.md`
</output>

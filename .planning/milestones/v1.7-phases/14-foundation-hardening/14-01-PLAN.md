---
phase: 14-foundation-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05

must_haves:
  truths:
    - "npm audit reports zero vulnerabilities"
    - "eslint-config-next version matches next@16.2.2"
    - "Error boundary <a> tags have eslint-disable comments with explanatory context"
    - "Velite is pinned to exact 0.3.1 with no caret prefix"
    - "No stale agent-* worktree directories exist under .claude/worktrees/"
  artifacts:
    - path: "package.json"
      provides: "Cleaned dependency declarations"
      contains: '"velite": "0.3.1"'
    - path: "package-lock.json"
      provides: "Locked dependency tree with audit fixes and version bumps"
  key_links:
    - from: "package.json"
      to: "package-lock.json"
      via: "npm install resolving declared versions"
      pattern: '"velite": "0.3.1"'
---

<objective>
Clean the dependency tree, pin Velite, bump eslint-config-next, and verify existing lint comment coverage -- all mechanical housekeeping before any migration work begins in later phases.

Purpose: Establish a clean, audit-free, lint-clean foundation so Phases 15-19 can focus on real migrations without noise from pre-existing issues.
Output: Updated package.json + package-lock.json with zero audit vulnerabilities, correct eslint-config-next version, and pinned Velite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-foundation-hardening/14-CONTEXT.md
@.planning/phases/14-foundation-hardening/14-RESEARCH.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix audit vulnerabilities, bump eslint-config-next, and pin Velite</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current dependency versions -- velite line 42, eslint-config-next line 53)
    - .planning/phases/14-foundation-hardening/14-RESEARCH.md (confirmed fix strategy)
  </read_first>
  <action>
    Three package.json edits plus npm operations, per user decisions D-01, D-02, D-04:

    1. **Edit package.json line 42** (per D-04 / FOUND-04): Change `"velite": "^0.3.1"` to `"velite": "0.3.1"` (remove the caret character only).

    2. **Edit package.json line 53** (per D-02 / FOUND-02): Change `"eslint-config-next": "^16.1.6"` to `"eslint-config-next": "^16.2.2"`.

    3. **Run `npm audit fix`** (per D-01 / FOUND-01): This resolves 3 vulnerabilities (brace-expansion, flatted, picomatch) by updating transitive dependencies in the lockfile. No `--force` flag, no `overrides` field needed.

    4. **Run `npm install`**: Picks up the two package.json edits (Velite pin and eslint-config-next bump) and updates the lockfile accordingly.

    5. **Verify the changes:**
       - `npm audit` must exit 0 (zero vulnerabilities)
       - `grep '"velite": "0.3.1"' package.json` must match (no caret)
       - `npm ls eslint-config-next` must show 16.2.x

    Order note: Run `npm audit fix` before `npm install` so the audit fixes land first, then `npm install` resolves the package.json edits on top. Both update the lockfile.
  </action>
  <verify>
    <automated>npm audit && grep -q '"velite": "0.3.1"' package.json && npm ls eslint-config-next --depth=0 | grep -q '16.2' && npm run lint && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `npm audit` exits with code 0 and reports "found 0 vulnerabilities"
    - `grep '"velite": "0.3.1"' package.json` prints exactly `    "velite": "0.3.1"` (no caret)
    - `grep '"eslint-config-next": "^16.2.2"' package.json` prints exactly `    "eslint-config-next": "^16.2.2"`
    - `npm ls eslint-config-next --depth=0` output contains `eslint-config-next@16.2.` (16.2.x)
    - `npm run lint` exits 0 with no errors
    - `npm run test` exits 0 with all tests passing
  </acceptance_criteria>
  <done>package.json has Velite pinned to exact 0.3.1, eslint-config-next at ^16.2.2, lockfile updated, zero audit vulnerabilities, lint and tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Verify existing lint comments and clean worktrees</name>
  <files></files>
  <read_first>
    - src/app/error.tsx (check for eslint-disable comment near <a> tag)
    - src/app/global-error.tsx (check for eslint-disable comment near <a> tag)
    - src/app/blog/[slug]/error.tsx (check for eslint-disable comment near <a> tag)
    - src/components/blog/mdx-content.tsx (reference pattern at line 29)
  </read_first>
  <action>
    Two verification-only steps per D-03 / FOUND-03 and D-05 / FOUND-05:

    1. **Verify FOUND-03 (eslint-disable comments):** Research confirmed all three error boundary files already have the correct comment. Read each file and verify the following exact comment exists on the line immediately before each `<a>` tag:
       ```
       {/* eslint-disable-next-line @next/next/no-html-link-for-pages -- plain <a> intentional: client-side routing may be broken in error state */}
       ```
       Files to check:
       - `src/app/error.tsx` (expected at line 26)
       - `src/app/global-error.tsx` (expected at line 32)
       - `src/app/blog/[slug]/error.tsx` (expected at line 28)

       If any file is MISSING the comment, add it following the exact pattern from `src/components/blog/mdx-content.tsx` line 29. If all present, this is a no-op -- log that FOUND-03 was already satisfied.

    2. **Clean worktrees (per D-05 / FOUND-05):** Run `rm -rf .claude/worktrees/agent-*` to remove any stale agent worktree directories. This is idempotent -- if none exist (research found the directory is currently empty), the command succeeds silently. Do NOT remove `.claude/worktrees/` itself or any other `.claude/` contents.

    3. **Final build verification:** Run `npm run build` to confirm the full build pipeline (velite + next build) works with all changes from Task 1.
  </action>
  <verify>
    <automated>grep -c "eslint-disable-next-line @next/next/no-html-link-for-pages" src/app/error.tsx src/app/global-error.tsx src/app/blog/\[slug\]/error.tsx | grep -v ':0$' | wc -l | grep -q '3' && ls .claude/worktrees/agent-* 2>&1 | grep -q "No such file\|no matches" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep "eslint-disable-next-line @next/next/no-html-link-for-pages" src/app/error.tsx` returns exactly 1 match
    - `grep "eslint-disable-next-line @next/next/no-html-link-for-pages" src/app/global-error.tsx` returns exactly 1 match
    - `grep "eslint-disable-next-line @next/next/no-html-link-for-pages" src/app/blog/\[slug\]/error.tsx` returns exactly 1 match
    - `ls .claude/worktrees/agent-*` returns "No such file or directory" or "no matches found"
    - `npm run build` exits 0 (velite compiles content, next build completes with all pages static)
  </acceptance_criteria>
  <done>All 3 error boundary files confirmed to have eslint-disable comments with explanatory context, no stale worktree directories exist, full build succeeds</done>
</task>

</tasks>

<verification>
Phase 14 verification checks (run after both tasks complete):

1. `npm audit` -- exits 0, "found 0 vulnerabilities"
2. `npm run lint` -- exits 0, zero errors
3. `npm run test` -- exits 0, all tests pass
4. `npm run build` -- exits 0, all pages static
5. `grep '"velite": "0.3.1"' package.json` -- matches (no caret)
6. `grep '"eslint-config-next": "^16.2.2"' package.json` -- matches
7. `grep -c "eslint-disable-next-line @next/next/no-html-link-for-pages" src/app/error.tsx src/app/global-error.tsx src/app/blog/\[slug\]/error.tsx` -- each file shows 1
8. `ls .claude/worktrees/agent-*` -- no matches / no such file
</verification>

<success_criteria>
- Zero npm audit vulnerabilities (FOUND-01)
- eslint-config-next at 16.2.x matching next@16.2.2 (FOUND-02)
- All error boundary files have eslint-disable comments with explanatory context (FOUND-03)
- Velite pinned to exact "0.3.1" in package.json (FOUND-04)
- No stale agent-* worktree directories (FOUND-05)
- Full test suite, lint, and build all pass clean
</success_criteria>

<output>
After completion, create `.planning/phases/14-foundation-hardening/14-01-SUMMARY.md`
</output>

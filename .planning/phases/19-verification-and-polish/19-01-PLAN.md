---
phase: 19-verification-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/blog/code-block-enhancer.tsx
  - src/components/blog/mdx-content.tsx
  - e2e/code-copy.spec.ts
  - e2e/mobile-menu.spec.ts
  - e2e/view-count.spec.ts
  - e2e/mobile-toc.spec.ts
autonomous: true
requirements:
  - VER-01
  - VER-02
  - VER-03
must_haves:
  truths:
    - "npm run lint exits 0 with no output (zero errors, zero warnings)"
    - "npm run test passes all 135+ unit tests"
    - "npm run build shows all pages as Static or SSG (Dynamic only for API routes and feed.xml)"
    - "npm run test:e2e passes all 4 specs on both desktop-chromium and mobile-chromium projects"
  artifacts:
    - path: "e2e/code-copy.spec.ts"
      provides: "Code copy E2E test passing under hardened CSP"
    - path: "e2e/mobile-menu.spec.ts"
      provides: "Mobile menu E2E test passing"
    - path: "e2e/view-count.spec.ts"
      provides: "View count E2E test passing"
    - path: "e2e/mobile-toc.spec.ts"
      provides: "Mobile TOC E2E test passing"
  key_links:
    - from: "src/proxy.ts"
      to: "all page responses"
      via: "middleware CSP header"
      pattern: "script-src.*'self'.*'unsafe-inline'"
    - from: "playwright.config.ts"
      to: "production build"
      via: "webServer: npm run build && npm run start"
      pattern: "command.*npm run build"
---

<objective>
Run all validation checks (lint, unit tests, build, E2E) against the hardened codebase and fix any failures found. This is the final quality gate for v1.7 -- confirming that the CSP hardening (Phase 16), CSS-variables theme (Phase 17), and React 19 lint cleanup (Phase 18) all work together without regressions.

Purpose: Prove the entire site works end-to-end under the hardened CSP with zero lint issues and full static generation preserved.
Output: All checks green -- ready to ship v1.7.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-verification-and-polish/19-CONTEXT.md
@.planning/phases/19-verification-and-polish/19-RESEARCH.md

@playwright.config.ts
@eslint.config.mjs
@src/proxy.ts
@e2e/code-copy.spec.ts
@e2e/mobile-menu.spec.ts
@e2e/view-count.spec.ts
@e2e/mobile-toc.spec.ts
@src/components/blog/code-block-enhancer.tsx
@src/components/blog/mdx-content.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run lint, unit tests, and build validation</name>
  <files>none expected -- validation only, files listed only if fixes needed</files>
  <read_first>
    - eslint.config.mjs (lint configuration with 3 React 19 warn-level rules)
    - src/proxy.ts (CSP policy -- confirm unsafe-eval absent from script-src)
    - next.config.ts (build configuration)
  </read_first>
  <action>
Run the three fast validation checks in order per D-05, D-03:

**Step 1 -- Lint (VER-03, per D-05):**
Run `npm run lint`. Check BOTH exit code AND stdout/stderr output. Exit code 0 is necessary but not sufficient -- if any warning text appears in the output, that is a D-05 violation. Expected result: exit 0, empty output.

If warnings appear (per D-06): fix the source code rather than adding eslint-disable comments. The only acceptable existing suppressions are: error boundary `<a>` tags (documented in CLAUDE.md) and animation orchestration effects in use-hero-animation (documented in Phase 18).

**Step 2 -- Unit tests (bonus sanity check):**
Run `npm run test`. Expected: 135+ tests pass, 0 failures. If any fail, fix the source code.

**Step 3 -- Build (VER-02, per D-03, D-04):**
Run `npm run build`. After build completes, inspect the route output table. Every page route must show as Static (circle symbol) or SSG (filled circle). The following Dynamic (f symbol) entries are EXPECTED and acceptable per D-03:
- `/api/views` (route handler)
- `/api/views/[slug]` (route handler)
- `/feed.xml` (route handler)

If ANY non-API, non-feed page shows as Dynamic (per D-04): investigate the cause -- likely an accidental `cookies()`, `headers()`, or `searchParams` call, or a missing `generateStaticParams`. Fix the root cause.

Per D-07: Do not introduce any feature additions, refactoring, or dependency updates. Only fix what is needed to pass the three checks.
  </action>
  <verify>
    <automated>npm run lint 2>&1 | tee /tmp/lint-output.txt && test ! -s /tmp/lint-output.txt && npm run test && npm run build 2>&1 | grep -E '^\s*(○|●|ƒ)\s+/' | grep -v '/api/' | grep -v '/feed' | grep 'ƒ' | wc -l | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits with code 0 AND produces zero lines of output (no warnings, no errors)
    - `npm run test` exits with code 0 and reports 0 failures
    - `npm run build` exits with code 0
    - Build output contains no `f` (Dynamic) symbol on page routes (only on `/api/*` and `/feed.xml`)
    - `src/proxy.ts` does NOT contain `unsafe-eval` in the script-src directive
  </acceptance_criteria>
  <done>Lint reports zero errors and zero warnings (VER-03). Unit tests all pass. Build output shows all pages as Static/SSG with only route handlers as Dynamic (VER-02).</done>
</task>

<task type="auto">
  <name>Task 2: Run full E2E suite and fix any failures</name>
  <files>
    - e2e/code-copy.spec.ts
    - e2e/mobile-menu.spec.ts
    - e2e/view-count.spec.ts
    - e2e/mobile-toc.spec.ts
    - src/components/blog/code-block-enhancer.tsx
    - src/components/blog/mdx-content.tsx
  </files>
  <read_first>
    - e2e/code-copy.spec.ts (copy button locator uses figure[data-rehype-pretty-code-figure] and getByRole button with name Copy code)
    - e2e/mobile-menu.spec.ts (uses devices Pixel 5 override, tests hamburger menu open/close/escape/navigation)
    - e2e/view-count.spec.ts (intercepts **/api/views/** routes to mock Redis, returns views: 42)
    - e2e/mobile-toc.spec.ts (mobile TOC expand/collapse, sticky behavior, auto-collapse)
    - src/components/blog/code-block-enhancer.tsx (DOM-based copy button injection -- wraps pre in div.group.relative, adds button inside)
    - src/components/blog/mdx-content.tsx (dangerouslySetInnerHTML rendering, CodeBlockEnhancer mounting)
    - playwright.config.ts (webServer runs npm run build && npm run start, 2 projects)
  </read_first>
  <action>
Run the full Playwright E2E suite per D-01 and D-02:

**Step 1 -- Run all E2E tests:**
Run `npm run test:e2e`. This will build the production app, start the server, and run all 4 specs against both desktop-chromium and mobile-chromium projects. The production server serves real CSP headers from `src/proxy.ts`, so passing E2E tests proves VER-01 (hardened CSP works).

**Step 2 -- If any test fails, diagnose and fix per D-01:**

Known risk area -- `code-copy.spec.ts`: Phase 16 changed copy buttons from React component overrides to DOM injection via `CodeBlockEnhancer`. The test finds `figure[data-rehype-pretty-code-figure]` then looks for a button with `getByRole('button', { name: 'Copy code' })` scoped to that figure. The `CodeBlockEnhancer` injects a `div.group.relative` wrapper around `<pre>` with a `<button>` inside, all within the `<figure>`. The scoped `getByRole` traverses descendants, so this SHOULD work. If it fails:
- Read the actual DOM structure by adding a `console.log(await codeBlock.innerHTML())` debug line
- Fix `code-block-enhancer.tsx` if the button is outside the figure scope
- Only modify the test selector if Phase 16 intentionally changed the DOM contract that the test was asserting

Known risk area -- `mobile-menu.spec.ts`: Uses `test.use({ ...devices['Pixel 5'] })` at file top. Should work on both projects because the device override forces mobile viewport. If the test fails on desktop-chromium because the hamburger button is not visible, the viewport override may not be taking effect. Fix: check if the Playwright version handles file-level device overrides correctly.

Known risk area -- `view-count.spec.ts`: Intercepts `**/api/views/**` and returns `{ views: 42 }`. Should not depend on Redis. If it fails with unexpected view counts, the route pattern is not matching.

Known risk area -- `mobile-toc.spec.ts`: Tests sticky TOC behavior on mobile. Uses graceful skip if no TOC found on the post. If it fails, check that the blog post navigated to actually has headings that generate a TOC.

Per D-01: Fix application code when tests fail. Only modify a test if the test was asserting behavior that was intentionally changed in phases 16-18.
Per D-07: No feature additions, no refactoring beyond what is needed to make tests pass.

**Step 3 -- Re-run after fixes:**
After any fix, re-run the full suite: `npm run test:e2e`. All 4 specs must pass on both projects.
  </action>
  <verify>
    <automated>npm run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:e2e` exits with code 0
    - Playwright output shows all tests passing in both `desktop-chromium` and `mobile-chromium` projects
    - `e2e/code-copy.spec.ts` passes (copy button visible, clipboard write works, copied state shown)
    - `e2e/mobile-menu.spec.ts` passes (open/close, escape dismiss, navigation works)
    - `e2e/view-count.spec.ts` passes (mocked view count of 42 displays correctly)
    - `e2e/mobile-toc.spec.ts` passes (expand/collapse, sticky, auto-collapse on navigation)
    - No test files were modified UNLESS the test was asserting behavior intentionally changed in phases 16-18
  </acceptance_criteria>
  <done>All 4 E2E specs pass on both desktop-chromium and mobile-chromium projects under the hardened CSP (VER-01). Combined with Task 1, all three VER requirements are satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSP header -> browser | CSP policy in proxy.ts restricts script execution; Phase 16 removed unsafe-eval |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Elevation of Privilege | CSP script-src | accept (validated) | unsafe-eval already removed in Phase 16; this phase validates via E2E that no runtime eval is needed. E2E tests run under real CSP headers from proxy.ts middleware. |
| T-19-02 | Tampering | Test mocking | accept | view-count.spec.ts intercepts API calls with page.route() which is standard Playwright practice; no production code is weakened |
</threat_model>

<verification>
Run the full validation suite in sequence:

```bash
# All four checks must pass
npm run lint          # VER-03: zero errors, zero warnings, no output
npm run test          # Bonus: unit test regression check
npm run build         # VER-02: all pages Static/SSG
npm run test:e2e      # VER-01: all E2E tests pass under hardened CSP
```

Verify no unintended changes:
- `git diff --stat` should show only files that needed fixing (ideally zero changes if everything passes)
- `src/proxy.ts` still has no `unsafe-eval` in script-src
</verification>

<success_criteria>
1. `npm run lint` exits 0 with zero output (VER-03)
2. `npm run test` exits 0 with 0 failures
3. `npm run build` shows all pages as Static/SSG, only API routes and feed.xml as Dynamic (VER-02)
4. `npm run test:e2e` exits 0 with all specs passing on both projects (VER-01)
5. No feature additions, no refactoring, no unnecessary dependency changes (D-07)
</success_criteria>

<output>
After completion, create `.planning/phases/19-verification-and-polish/19-01-SUMMARY.md`
</output>

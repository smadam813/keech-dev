---
phase: 01-foundation-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - src/app/layout.tsx
  - src/app/globals.css
  - src/app/page.tsx
  - src/lib/fonts.ts
  - src/lib/utils.ts
autonomous: true

must_haves:
  truths:
    - "Next.js 15 app builds successfully with no errors"
    - "Tailwind v4 utilities work (bg-background, text-foreground, shadow-brutal)"
    - "Space Grotesk renders for headings, Inter for body text"
    - "Color palette passes WCAG AA contrast (4.5:1 for text)"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Design tokens via @theme"
      contains: "@theme"
    - path: "src/lib/fonts.ts"
      provides: "Font configuration"
      exports: ["spaceGrotesk", "inter"]
    - path: "src/app/layout.tsx"
      provides: "Root layout with fonts applied"
      contains: "spaceGrotesk.variable"
  key_links:
    - from: "src/lib/fonts.ts"
      to: "src/app/layout.tsx"
      via: "font variable import and className application"
      pattern: "spaceGrotesk\\.variable.*inter\\.variable"
    - from: "src/app/globals.css"
      to: "tailwind utilities"
      via: "@theme block defining color-*, shadow-*, border-*"
      pattern: "--color-background.*--shadow-brutal"
---

<objective>
Initialize Next.js 15 project with Tailwind v4 design system for neobrutalist cosmic theme.

Purpose: Establish the foundation that all other components build upon. Without the design tokens, fonts, and build system, nothing else can be created.

Output: Working Next.js 15 app with complete neobrutalist design system (color palette, typography, shadow tokens) that passes WCAG contrast validation.
</objective>

<execution_context>
@/home/smada/.claude/get-shit-done/workflows/execute-plan.md
@/home/smada/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-design/01-CONTEXT.md
@.planning/phases/01-foundation-design/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 15 with Tailwind v4</name>
  <files>
    package.json
    tsconfig.json
    next.config.ts
    postcss.config.mjs
    src/app/layout.tsx
    src/app/page.tsx
    src/lib/utils.ts
  </files>
  <action>
Create Next.js 15 project with App Router and Tailwind v4:

1. Initialize with: `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --turbopack`
   - Answer prompts: Use src/ directory: Yes, App Router: Yes
   - If directory not empty, allow overwrite of README

2. Upgrade to Tailwind v4 (create-next-app may install v3):
   - Check package.json for tailwind version
   - If v3, run: `npm install tailwindcss@latest @tailwindcss/postcss@latest postcss@latest`
   - Delete tailwind.config.ts if exists (v4 uses CSS-first config)
   - Update postcss.config.mjs to use @tailwindcss/postcss plugin

3. Create src/lib/utils.ts with cn() helper:
   ```typescript
   import { type ClassValue, clsx } from "clsx"
   import { twMerge } from "tailwind-merge"

   export function cn(...inputs: ClassValue[]) {
     return twMerge(clsx(inputs))
   }
   ```

4. Install utilities: `npm install clsx tailwind-merge`

5. Verify build: `npm run build` should complete without errors
  </action>
  <verify>
    `npm run build` completes successfully
    `npm run dev` starts without errors
    Visiting http://localhost:3000 shows the default Next.js page
  </verify>
  <done>
    Next.js 15 project initialized with Tailwind v4, builds successfully, dev server runs
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure Design System Tokens</name>
  <files>
    src/app/globals.css
    src/lib/fonts.ts
    src/app/layout.tsx
  </files>
  <action>
Set up the neobrutalist cosmic design system:

1. Create src/lib/fonts.ts with Space Grotesk and Inter:
   ```typescript
   import { Space_Grotesk, Inter } from 'next/font/google'

   export const spaceGrotesk = Space_Grotesk({
     variable: '--font-display',
     subsets: ['latin'],
     weight: ['400', '500', '600', '700'],
     display: 'swap',
   })

   export const inter = Inter({
     variable: '--font-body',
     subsets: ['latin'],
     weight: ['400', '500', '600', '700'],
     display: 'swap',
   })
   ```

2. Replace src/app/globals.css with Tailwind v4 @theme configuration:
   ```css
   @import "tailwindcss";

   @theme {
     /* Cosmic neobrutalist palette */
     --color-background: #E8B4B8;
     --color-foreground: #000000;
     --color-accent: #2D8B8B;
     --color-accent-hover: #3AA3A3;
     --color-surface: #F5E6E8;
     --color-muted: #666666;

     /* Hard offset shadows - neobrutalist signature */
     --shadow-brutal: 4px 4px 0 0 #000000;
     --shadow-brutal-lg: 6px 6px 0 0 #000000;
     --shadow-brutal-hover: 2px 2px 0 0 #000000;

     /* Border widths */
     --border-brutal: 3px;
     --border-brutal-lg: 4px;
   }

   @theme inline {
     --font-display: var(--font-display);
     --font-body: var(--font-body);
   }

   @layer base {
     html {
       @apply font-body bg-background text-foreground antialiased;
     }

     h1, h2, h3, h4, h5, h6 {
       @apply font-display;
     }
   }
   ```

3. Update src/app/layout.tsx to apply fonts:
   ```tsx
   import type { Metadata } from "next";
   import { spaceGrotesk, inter } from "@/lib/fonts";
   import "./globals.css";

   export const metadata: Metadata = {
     title: "keech.dev",
     description: "Personal portfolio and blog",
   };

   export default function RootLayout({
     children,
   }: Readonly<{
     children: React.ReactNode;
   }>) {
     return (
       <html lang="en">
         <body className={`${spaceGrotesk.variable} ${inter.variable}`}>
           {children}
         </body>
       </html>
     );
   }
   ```

4. Update src/app/page.tsx to test design tokens:
   ```tsx
   export default function Home() {
     return (
       <main className="min-h-screen bg-background p-8">
         <h1 className="font-display text-6xl font-bold mb-4">keech.dev</h1>
         <p className="font-body text-xl text-muted mb-8">Design system test</p>
         <div className="inline-block bg-accent text-white px-6 py-3 border-[3px] border-foreground shadow-brutal">
           Neobrutalist Button
         </div>
       </main>
     );
   }
   ```

5. Validate WCAG contrast - install colorable and check:
   ```bash
   npm install -D colorable
   ```

   Create scripts/validate-colors.mjs:
   ```javascript
   import colorable from 'colorable';

   const palette = {
     background: '#E8B4B8',
     foreground: '#000000',
     accent: '#2D8B8B',
     surface: '#F5E6E8',
     muted: '#666666',
   };

   const result = colorable(palette, { threshold: 4.5 });
   let allPass = true;

   console.log('Color Contrast Report (WCAG AA = 4.5:1 minimum)\n');

   result.forEach((color) => {
     console.log(`${color.name}:`);
     color.combinations.forEach((combo) => {
       const pass = combo.contrast >= 4.5;
       if (!pass) allPass = false;
       console.log(`  vs ${combo.name}: ${combo.contrast.toFixed(2)} [${pass ? 'PASS' : 'FAIL'}]`);
     });
   });

   if (!allPass) {
     console.log('\nWARNING: Some combinations fail WCAG AA. Adjust palette if using these pairs for text.');
   } else {
     console.log('\nAll critical text/background combinations pass WCAG AA.');
   }
   ```

6. Run contrast validation: `node scripts/validate-colors.mjs`
   - foreground (#000) on background (#E8B4B8) should pass (critical)
   - foreground (#000) on surface (#F5E6E8) should pass (critical)
   - Note: Not all pairs need to pass, only those used for text
  </action>
  <verify>
    `npm run dev` shows dusty pink background with black text
    `h1` uses Space Grotesk (visually different from body text)
    Test button shows hard shadow offset (no blur)
    `node scripts/validate-colors.mjs` runs without errors
  </verify>
  <done>
    Design tokens configured: colors, shadows, borders, fonts all working
    Page displays with dusty pink background, Space Grotesk headings, Inter body
    Color contrast validated for critical text/background pairs
  </done>
</task>

</tasks>

<verification>
Run full verification after all tasks:

1. `npm run build` - Builds without errors
2. `npm run dev` - Dev server starts
3. Visit http://localhost:3000:
   - Background is dusty pink (#E8B4B8)
   - Heading "keech.dev" is in Space Grotesk
   - Body text is in Inter
   - Test button has hard 4px offset shadow, no blur
4. `node scripts/validate-colors.mjs` - Shows contrast report
</verification>

<success_criteria>
- Next.js 15 + Tailwind v4 project builds and runs
- Design tokens work: bg-background, text-foreground, shadow-brutal classes function
- Fonts load: Space Grotesk for display, Inter for body
- WCAG contrast validated for primary text/background combinations
- No TypeScript or build errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design/01-01-SUMMARY.md`
</output>

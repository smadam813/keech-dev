---
phase: 07-norse-typography
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/app/globals.css
  - src/components/layout/header.tsx
autonomous: false

must_haves:
  truths:
    - "h1 and h2 headings render in Bold weight; h3-h6 render in Regular weight"
    - "Site name 'keech.dev' in header renders in Regular weight"
    - "Navigation links (Home, Blog, Projects, About) render in Regular weight on both desktop and mobile"
    - "Heading text at all sizes (text-lg through text-9xl) looks intentional with tuned spacing"
    - "No visible text reflow or layout shift from font swap at throttled 3G"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Split heading weight rules, prose weight updates, letter-spacing and line-height tuning"
      contains: "font-normal"
    - path: "src/components/layout/header.tsx"
      provides: "Updated weight classes for site name and navigation"
      contains: "font-normal"
  key_links:
    - from: "src/app/globals.css"
      to: "src/lib/fonts.ts"
      via: "CSS variable --font-display consumed by font-display utility"
      pattern: "font-display"
    - from: "src/components/layout/header.tsx"
      to: "src/lib/fonts.ts"
      via: "Tailwind font-display utility resolves to --font-display variable"
      pattern: "font-display"
---

<objective>
Tune Norse font weights, letter-spacing, and line-height across all heading levels, navigation, and site branding per the locked weight hierarchy decisions.

Purpose: Make the Norse font look intentional and polished at every size, not just "swapped in" -- the font character should shine through proper typographic tuning.
Output: Updated globals.css with split weight rules and spacing tuning, updated header.tsx with corrected weight classes.
</objective>

<execution_context>
@/home/smada/.claude/get-shit-done/workflows/execute-plan.md
@/home/smada/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-norse-typography/07-01-SUMMARY.md
@src/app/globals.css
@src/components/layout/header.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply weight hierarchy and typography tuning</name>
  <files>
    src/app/globals.css
    src/components/layout/header.tsx
  </files>
  <action>
**LOCKED DECISIONS (implement exactly):**
- Bold weight for h1 and h2 ONLY
- Regular weight for h3-h6
- Site name "keech.dev" in header: Regular weight
- Navigation links (Blog, Projects, About): Regular weight
- Mobile menu nav links: Regular weight (currently font-bold)

**1. Update globals.css base layer -- split heading weight rules:**

Replace the single `h1, h2, h3, h4, h5, h6 { @apply font-display; }` rule with:
```css
h1, h2 {
  @apply font-display font-bold;
}
h3, h4, h5, h6 {
  @apply font-display font-normal;
}
```

**2. Update globals.css prose heading weights:**

- `.prose h2`: keep `font-bold` (h2 = Bold per decision)
- `.prose h3`: change `font-bold` to `font-normal` (h3 = Regular per decision)
- `.prose h4`: change `font-bold` to `font-normal` (h4 = Regular per decision)

**3. Add letter-spacing and line-height tuning to globals.css:**

Add typography tuning rules in the base layer, after the heading weight rules:
```css
/* Norse font typography tuning */
h1 {
  letter-spacing: -0.02em;
  line-height: 1.1;
}
h2 {
  letter-spacing: -0.01em;
  line-height: 1.2;
}
h3, h4, h5, h6 {
  letter-spacing: 0.01em;
  line-height: 1.3;
}
```

Also add tuning for the prose responsive breakpoint (mobile h2/h3 already have size overrides, ensure line-height matches).

**4. Update header.tsx weight classes:**

- Site name `<span>`: change `font-bold` to `font-normal` (line ~94)
- Desktop nav links: change `font-medium` to `font-normal` (line ~106)
- Mobile menu nav links: change `font-bold` to `font-normal` (line ~163)

**5. Inline bold handling (discretionary):**

Let `<strong>` inside h3-h6 naturally render in Norse Bold (700). No special CSS override needed -- the browser will request the bold weight from the font family, which Plan 01 registered as weight 700. This gives authors the option to emphasize within lighter headings.

**6. Check other components for weight overrides:**

Scan these components that use `font-display` and verify weights are appropriate:
- `src/app/page.tsx` -- h1 with `font-bold`: CORRECT (h1 = Bold)
- `src/app/not-found.tsx` -- h1 with `font-bold`: CORRECT (h1 = Bold)
- `src/app/about/page.tsx` -- h1 with `font-bold`: CORRECT (h1 = Bold)
- `src/app/blog/page.tsx` -- h1 with `font-bold`: CORRECT (h1 = Bold)
- `src/app/projects/page.tsx` -- h1 with `font-bold`: CORRECT (h1 = Bold)
- `src/app/blog/[slug]/page.tsx` -- h1 with `font-bold`: CORRECT
- `src/app/projects/[slug]/page.tsx` -- h1 with `font-bold`: CORRECT
- `src/components/blog/post-card.tsx` -- h2 with `font-bold`: CORRECT (h2 = Bold)
- `src/components/projects/project-card.tsx` -- h2 with `font-bold`: CORRECT
- `src/components/blog/toc.tsx` -- h2 with `font-bold`: CORRECT
- `src/app/not-found.tsx` -- link with `font-semibold font-display`: This is a CTA button, not a heading. Change `font-semibold` to `font-normal` to match the nav link style since it uses font-display.
- `src/components/layout/footer.tsx` -- `font-display text-sm`: No weight specified, inherits normal. CORRECT.
- `src/app/about/page.tsx` -- `font-display text-lg` on "Photo" label: No weight specified, inherits normal. CORRECT.

No other files need changes.
  </action>
  <verify>
    - `grep "font-bold" src/components/layout/header.tsx` returns 0 matches (no bold in header)
    - `grep "font-medium" src/components/layout/header.tsx` returns 0 matches (no medium in header)
    - `grep "font-normal" src/components/layout/header.tsx` returns at least 3 matches (site name, desktop nav, mobile nav)
    - `grep "font-normal" src/app/globals.css` returns matches for h3-h6 base rule and prose h3/h4
    - `grep "letter-spacing" src/app/globals.css` returns matches for tuning rules
    - `npm run build` completes without errors
  </verify>
  <done>
    Bold weight is exclusive to h1 and h2. All h3-h6 headings, site name, and navigation use Regular weight. Letter-spacing and line-height are tuned for the Norse font at all heading sizes. Build passes clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Norse typography</name>
  <what-built>Norse font integrated as display typeface with weight hierarchy (Bold for h1/h2, Regular for everything else) and letter-spacing/line-height tuning across all heading sizes.</what-built>
  <how-to-verify>
1. Run `npm run dev` and open http://localhost:3000
2. **Home page:** Verify "keech.dev" renders in Norse Regular (not bold). The large heading text should feel distinctly Norse/runic.
3. **Navigation:** Desktop nav links (Home, Blog, Projects, About) render in Norse Regular weight. On mobile (resize to <768px), hamburger menu links also render in Norse Regular.
4. **Blog page** (http://localhost:3000/blog): Verify h1 "Blog" is Bold. Click into any post.
5. **Blog post:** Verify h1 is Bold, h2 subheadings are Bold, h3 subheadings are Regular (lighter). Check that letter-spacing feels natural at each size -- not too tight or too loose.
6. **Projects page** (http://localhost:3000/projects): Verify h1 "Projects" is Bold. Project card h2 titles are Bold.
7. **Throttle test:** Open DevTools > Network > Throttle to "Slow 3G". Hard refresh. Watch for layout shift -- text should not visibly reflow or jump when Norse loads.
8. **404 page** (http://localhost:3000/anything): Verify "404" renders in Norse Bold.
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 7, or describe any visual issues that need fixing.</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. Weight hierarchy matches locked decisions: Bold for h1/h2, Regular for h3-h6/nav/site-name
3. Letter-spacing and line-height tuning applied in globals.css
4. No remaining `font-medium` or `font-bold` on navigation or site name in header.tsx
5. Visual verification by user confirms Norse font looks intentional at all sizes
</verification>

<success_criteria>
- h1/h2 elements render in Norse Bold; h3-h6 elements render in Norse Regular
- Site name and all navigation text render in Norse Regular
- Letter-spacing is tuned: slightly tighter for large headings, slightly wider for smaller headings
- No layout shift visible at throttled 3G speeds
- User approves visual appearance across all page types
</success_criteria>

<output>
After completion, create `.planning/phases/07-norse-typography/07-02-SUMMARY.md`
</output>

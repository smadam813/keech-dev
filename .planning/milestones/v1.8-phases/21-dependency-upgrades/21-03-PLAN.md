---
phase: 21-dependency-upgrades
plan: 03
type: execute
wave: 3
depends_on:
  - 21-02
files_modified:
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - DEPS-04

must_haves:
  truths:
    - "@vercel/analytics is at 2.x in package.json"
    - "The Analytics component renders without errors"
    - "No CSP violations appear in the build output"
    - "npm run build && npm run test && npm run lint all pass"
  artifacts:
    - path: "package.json"
      provides: "@vercel/analytics upgraded to ^2.0.1"
      contains: "@vercel/analytics"
    - path: "package-lock.json"
      provides: "Regenerated lock file"
  key_links:
    - from: "src/app/layout.tsx"
      to: "@vercel/analytics/next"
      via: "Analytics component import"
      pattern: "from '@vercel/analytics/next'"
    - from: "src/proxy.ts"
      to: "va.vercel-scripts.com"
      via: "CSP allowlist"
      pattern: "va.vercel-scripts.com"
---

<objective>
Upgrade @vercel/analytics to 2.x as the third risk-tiered batch (per D-01).

Purpose: Bring analytics to the current major version. Low-risk upgrade -- import path preserved, no code changes expected.
Output: @vercel/analytics 2.x installed, build verified, CSP compatibility confirmed at build level.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-dependency-upgrades/21-CONTEXT.md
@.planning/phases/21-dependency-upgrades/21-RESEARCH.md
@package.json
@src/app/layout.tsx
@src/proxy.ts
</context>

<interfaces>
<!-- Key integration points for @vercel/analytics 2.x -->

From src/app/layout.tsx:
```typescript
import { Analytics } from '@vercel/analytics/next'
// Used as: <Analytics />
```
This import path exists in @vercel/analytics 2.x (verified in RESEARCH.md).

From src/proxy.ts:
```typescript
"script-src 'self' 'unsafe-inline' https://va.vercel-scripts.com",
"connect-src 'self' https://va.vercel-scripts.com",
```
CSP domain `va.vercel-scripts.com` is expected to remain unchanged in 2.x.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade @vercel/analytics to 2.x</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current @vercel/analytics version)
    - src/app/layout.tsx (Analytics component import -- verify import path)
    - src/proxy.ts (CSP headers -- verify va.vercel-scripts.com in allowlist)
    - .planning/phases/21-dependency-upgrades/21-RESEARCH.md (Pattern 3: @vercel/analytics 2.x section)
  </read_first>
  <action>
Upgrade @vercel/analytics to 2.x (per DEPS-04):

```bash
npm install @vercel/analytics@^2.0.1
```

Per RESEARCH.md Pattern 3:
- The `@vercel/analytics/next` import path is preserved in 2.x -- no code changes to layout.tsx.
- The `endpoint` option was renamed to `eventEndpoint`/`viewEndpoint`, but this project uses no custom endpoints -- not affected.
- License changed from MPL-2.0 to MIT -- no code impact.

After install, verify import path still resolves:
```bash
node -e "import('@vercel/analytics/next').then(m => console.log('OK:', Object.keys(m))).catch(e => console.error('FAIL:', e.message))"
```

Then run full pipeline validation (per D-06):
```bash
npm run build && npm run test && npm run lint && npm audit
```

All commands must pass with zero errors. The CSP in src/proxy.ts should NOT need changes (domain unchanged per research assumption A1), but this will be fully confirmed when the site is deployed and tested in a browser.
  </action>
  <verify>
    <automated>npm run build && npm run test && npm run lint && npm audit</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"@vercel/analytics": "^2.0.1"`
    - `npm run build` exits 0
    - `npm run test` exits 0
    - `npm run lint` exits 0
    - `npm audit` reports 0 vulnerabilities
    - src/app/layout.tsx is UNCHANGED (import path `@vercel/analytics/next` works in 2.x)
    - src/proxy.ts is UNCHANGED (CSP domain unchanged)
  </acceptance_criteria>
  <done>@vercel/analytics 2.x installed. Build, tests, lint, and audit all pass. No code changes needed. CSP compatibility confirmed at build level.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> package-lock.json | New analytics package fetched from npm |
| analytics script -> CSP | Analytics 2.x script must load from CSP-allowed domain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Information Disclosure | CSP bypass | accept | Domain assumed unchanged (A1 in RESEARCH.md); will be verified in browser post-deploy. If violated, update src/proxy.ts CSP. |
| T-21-05 | Tampering | npm package | mitigate | Run `npm audit` after install |
</threat_model>

<verification>
- `npm run build && npm run test && npm run lint && npm audit` all exit 0
- package.json shows @vercel/analytics at ^2.0.1
</verification>

<success_criteria>
@vercel/analytics 2.x installed with no code changes. Build and all validation passes. CSP assumed compatible (browser verification deferred to deployment).
</success_criteria>

<output>
After completion, create `.planning/phases/21-dependency-upgrades/21-03-SUMMARY.md`
</output>

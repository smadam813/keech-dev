---
phase: 18-react-19-lint-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/use-media-query.ts
  - src/hooks/use-media-query.test.ts
  - src/hooks/use-view-store.ts
  - src/hooks/use-view-store.test.ts
autonomous: true
requirements: [RQ-01, RQ-02]
must_haves:
  truths:
    - "useMediaQuery hook returns boolean reflecting window.matchMedia state"
    - "useMediaQuery hook responds to media query changes without page reload"
    - "useMediaQuery hook returns false during SSR (getServerSnapshot)"
    - "useViewStore hook reads localStorage view counts synchronously during render"
    - "useViewStore hook returns null during SSR (getServerSnapshot)"
    - "useViewStore hook listens to storage events for cross-tab sync"
  artifacts:
    - path: "src/hooks/use-media-query.ts"
      provides: "Shared useSyncExternalStore-based media query hook"
      exports: ["useMediaQuery"]
    - path: "src/hooks/use-media-query.test.ts"
      provides: "Tests for useMediaQuery hook"
      contains: "useMediaQuery"
    - path: "src/hooks/use-view-store.ts"
      provides: "Shared useSyncExternalStore-based localStorage view store hook"
      exports: ["useViewStore"]
    - path: "src/hooks/use-view-store.test.ts"
      provides: "Tests for useViewStore hook"
      contains: "useViewStore"
  key_links:
    - from: "src/hooks/use-media-query.ts"
      to: "react"
      via: "import { useSyncExternalStore }"
      pattern: "useSyncExternalStore"
    - from: "src/hooks/use-view-store.ts"
      to: "react"
      via: "import { useSyncExternalStore, useCallback }"
      pattern: "useSyncExternalStore"
---

<objective>
Create the two shared hooks that Phase 18 consumers will depend on: `useMediaQuery` (for matchMedia subscriptions) and `useViewStore` (for localStorage view count reads). Both use `useSyncExternalStore` per React 19 idioms.

Purpose: These hooks replace the useState + useEffect anti-patterns that produce react-hooks/set-state-in-effect lint warnings. They must exist and be tested before Plan 02 migrates consumers.
Output: Two new hook files with full test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-react-19-lint-cleanup/18-CONTEXT.md
@.planning/phases/18-react-19-lint-cleanup/18-RESEARCH.md

<interfaces>
<!-- Existing hook patterns in this project -->
From src/hooks/use-hero-animation.ts:
```typescript
'use client'
import { useRef, useState, useEffect, useCallback, type RefObject } from 'react'
export function useHeroAnimation({ imgRef }: UseHeroAnimationOptions): UseHeroAnimationResult
```

From src/lib/views.ts:
```typescript
export function getCachedViews(slug: string): number | null
export function setCachedViews(slug: string, count: number): void
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create useViewStore hook with tests</name>
  <files>src/hooks/use-view-store.ts, src/hooks/use-view-store.test.ts</files>
  <read_first>
    - src/lib/views.ts (getCachedViews pattern to replicate in getSnapshot)
    - src/components/blog/view-counter.tsx (current consumer pattern being replaced)
    - src/components/blog/listing-view-counts.tsx (current consumer pattern being replaced)
    - src/hooks/use-hero-animation.test.ts (existing test patterns in this project)
  </read_first>
  <behavior>
    - Test 1: useViewStore returns null when localStorage has no entry for slug
    - Test 2: useViewStore returns numeric value when localStorage has `views:${slug}` entry
    - Test 3: useViewStore returns null during SSR (getServerSnapshot)
    - Test 4: useViewStore returns null when localStorage throws (e.g., SecurityError)
    - Test 5: useViewStore updates when storage event fires (cross-tab sync)
  </behavior>
  <action>
Per D-01, D-02, D-03: Create `src/hooks/use-view-store.ts` with `'use client'` directive.

**Hook signature:**
```typescript
export function useViewStore(slug: string): number | null
```

**Implementation details:**
- Import `useSyncExternalStore` and `useCallback` from `'react'`
- Define module-level `subscribeToStorage` function (stable reference per react.dev docs):
  ```typescript
  function subscribeToStorage(callback: () => void) {
    window.addEventListener('storage', callback)
    return () => window.removeEventListener('storage', callback)
  }
  ```
- `getSnapshot` wrapped in `useCallback` with `[slug]` dependency:
  - Reads `localStorage.getItem(`views:${slug}`)` inside try/catch
  - Returns `Number(raw)` if raw !== null, else `null`
  - Returns primitive (number | null) to satisfy Object.is comparison
- `getServerSnapshot` returns `null` (per D-03)
- Call: `useSyncExternalStore(subscribeToStorage, getSnapshot, () => null)`

**Test file** `src/hooks/use-view-store.test.ts`:
- Import `renderHook` from `@testing-library/react`
- Mock `localStorage` via `vi.stubGlobal` or direct property stubs
- Test the 5 behaviors listed above
- For storage event test: dispatch `new StorageEvent('storage')` on window after render
  </action>
  <verify>
    <automated>npx vitest run src/hooks/use-view-store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/hooks/use-view-store.ts` exists and starts with `'use client'`
    - File contains `import { useSyncExternalStore, useCallback } from 'react'`
    - File contains `export function useViewStore(slug: string): number | null`
    - File contains `window.addEventListener('storage', callback)` in subscribe function
    - File contains `localStorage.getItem(`views:${slug}`)` in getSnapshot
    - File contains `() => null` as getServerSnapshot argument
    - `npx vitest run src/hooks/use-view-store.test.ts` exits 0
  </acceptance_criteria>
  <done>useViewStore hook reads localStorage via useSyncExternalStore, returns null on server, listens to storage events. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create useMediaQuery hook with tests</name>
  <files>src/hooks/use-media-query.ts, src/hooks/use-media-query.test.ts</files>
  <read_first>
    - src/hooks/use-hero-animation.ts (current matchMedia pattern being replaced, lines 38-45)
    - src/components/ui/scroll-reveal.tsx (current matchMedia pattern being replaced, lines 17-18)
    - src/hooks/use-hero-animation.test.ts (existing test patterns)
  </read_first>
  <behavior>
    - Test 1: useMediaQuery returns false by default (no match)
    - Test 2: useMediaQuery returns true when matchMedia.matches is true
    - Test 3: useMediaQuery returns false during SSR (getServerSnapshot)
    - Test 4: useMediaQuery responds to change events on the MediaQueryList
  </behavior>
  <action>
Per D-05, D-06: Create `src/hooks/use-media-query.ts` with `'use client'` directive.

**Hook signature:**
```typescript
export function useMediaQuery(query: string): boolean
```

**Implementation details:**
- Import `useSyncExternalStore` from `'react'`
- `subscribe` function: creates `window.matchMedia(query)`, adds `'change'` event listener via `addEventListener`, returns cleanup that calls `removeEventListener`
- `getSnapshot`: returns `window.matchMedia(query).matches`
- `getServerSnapshot`: returns `false` (per D-06 -- assume no reduced motion on server)
- Note: subscribe creates a closure capturing `query`. This is fine since all callers pass static string literals (verified: only `'(prefers-reduced-motion: reduce)'` is used).

**Complete implementation:**
```typescript
'use client'

import { useSyncExternalStore } from 'react'

export function useMediaQuery(query: string): boolean {
  return useSyncExternalStore(
    (callback) => {
      const mq = window.matchMedia(query)
      mq.addEventListener('change', callback)
      return () => mq.removeEventListener('change', callback)
    },
    () => window.matchMedia(query).matches,
    () => false
  )
}
```

**Test file** `src/hooks/use-media-query.test.ts`:
- Mock `window.matchMedia` to return a controllable object with `matches`, `addEventListener`, `removeEventListener`
- Test the 4 behaviors listed above
- For change event test: capture the addEventListener callback, call it, verify re-render with new value
  </action>
  <verify>
    <automated>npx vitest run src/hooks/use-media-query.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/hooks/use-media-query.ts` exists and starts with `'use client'`
    - File contains `import { useSyncExternalStore } from 'react'`
    - File contains `export function useMediaQuery(query: string): boolean`
    - File contains `mq.addEventListener('change', callback)` in subscribe
    - File contains `window.matchMedia(query).matches` in getSnapshot
    - File contains `() => false` as getServerSnapshot argument
    - `npx vitest run src/hooks/use-media-query.test.ts` exits 0
  </acceptance_criteria>
  <done>useMediaQuery hook subscribes to matchMedia via useSyncExternalStore, returns false on server, responds to change events. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries in this plan. Both hooks read from client-side browser APIs (localStorage, matchMedia) with no user input, no network calls, no server-side data.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | I (Info Disclosure) | useViewStore getSnapshot | accept | localStorage view counts are non-sensitive (public page view numbers). No PII stored. |
</threat_model>

<verification>
- `npx vitest run src/hooks/use-view-store.test.ts` exits 0
- `npx vitest run src/hooks/use-media-query.test.ts` exits 0
- `npm run test -- --run` (full suite) exits 0
</verification>

<success_criteria>
- Two new hook files exist in src/hooks/ with 'use client' directives
- Both hooks use useSyncExternalStore (not useState + useEffect)
- Both hooks provide getServerSnapshot (null for views, false for media query)
- All new tests pass
- Existing test suite unaffected (all 126+ tests still pass)
</success_criteria>

<output>
After completion, create `.planning/phases/18-react-19-lint-cleanup/18-01-SUMMARY.md`
</output>

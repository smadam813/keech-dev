---
phase: 20-dead-code-test-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/blog/copy-button.tsx
  - src/components/blog/copy-button.test.tsx
  - src/lib/security-headers.test.ts
  - src/proxy.test.ts
  - src/app/error.test.tsx
  - tsconfig.json
autonomous: true
requirements:
  - HYGN-01
  - HYGN-02
  - HYGN-03
  - HYGN-04

must_haves:
  truths:
    - "CopyButton component and its test no longer exist in the codebase"
    - "lucide-react remains in package.json (6 other consumers confirmed)"
    - "Security headers test lives at src/proxy.test.ts next to src/proxy.ts and passes"
    - "npx tsc --noEmit reports zero errors across the entire codebase"
    - "npm run test passes with exactly 132 tests"
  artifacts:
    - path: "src/proxy.test.ts"
      provides: "Security headers test co-located with proxy module"
      contains: "from './proxy'"
    - path: "tsconfig.json"
      provides: "TypeScript config with vitest globals type"
      contains: "vitest/globals"
  key_links:
    - from: "src/proxy.test.ts"
      to: "src/proxy.ts"
      via: "relative import ./proxy"
      pattern: "from './proxy'"
    - from: "tsconfig.json"
      to: "node_modules/vitest/globals.d.ts"
      via: "compilerOptions.types array"
      pattern: "vitest/globals"
---

<objective>
Remove orphaned CopyButton files, relocate misplaced security-headers test to co-locate with its source, and fix tsconfig so `tsc --noEmit` reports zero errors.

Purpose: Establish a clean baseline (zero dead code, co-located tests, zero false tsc errors) before dependency upgrades in Phase 21.
Output: A codebase where `npm run test` passes with 132 tests and `npx tsc --noEmit` reports 0 errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-dead-code-test-hygiene/20-CONTEXT.md
@.planning/phases/20-dead-code-test-hygiene/20-RESEARCH.md

<interfaces>
<!-- No new interfaces created. Key files being modified: -->

From tsconfig.json (current state - no types array):
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["dom", "dom.iterable", "esnext"],
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "incremental": true,
    "paths": { "@/*": ["./src/*"], "@/.velite": ["./.velite"] }
  }
}
```

From src/lib/security-headers.test.ts (current import - must change):
```typescript
import proxy, { config } from '../../src/proxy'
```

From src/app/error.test.tsx (current import - shadows global Error):
```typescript
import Error from './error'
// Used as: <Error error={new Error('boom')} reset={() => {}} />
// The `new Error('boom')` calls the React component, not global Error
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete orphaned CopyButton files (HYGN-01, HYGN-02)</name>
  <files>src/components/blog/copy-button.tsx, src/components/blog/copy-button.test.tsx</files>
  <read_first>
    - src/components/blog/copy-button.tsx
    - src/components/blog/copy-button.test.tsx
    - package.json
  </read_first>
  <action>
Delete two orphaned files per D-01:
1. `rm src/components/blog/copy-button.tsx`
2. `rm src/components/blog/copy-button.test.tsx`

Per D-02, do NOT remove lucide-react from package.json. Research confirmed 6 other source files import it: header.tsx, footer.tsx, mobile-toc.tsx, project-card.tsx, blog/[slug]/page.tsx, projects/[slug]/page.tsx.

After deletion, run `npm run test -- --run` to confirm test suite passes and the count drops from 135 to 132 (copy-button.test.tsx contained exactly 3 tests).
  </action>
  <verify>
    <automated>npm run test -- --run 2>&1 | grep -E "Tests|test files" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/blog/copy-button.tsx` does not exist (ls returns error)
    - `src/components/blog/copy-button.test.tsx` does not exist (ls returns error)
    - `grep -r "lucide-react" package.json` still shows lucide-react as a dependency
    - `npm run test -- --run` passes with 132 tests (not 135)
  </acceptance_criteria>
  <done>Both CopyButton files deleted. lucide-react confirmed present. Test suite passes with 132 tests.</done>
</task>

<task type="auto">
  <name>Task 2: Relocate security-headers test to src/proxy.test.ts (HYGN-03)</name>
  <files>src/lib/security-headers.test.ts, src/proxy.test.ts</files>
  <read_first>
    - src/lib/security-headers.test.ts
    - src/proxy.ts
  </read_first>
  <action>
Relocate the test file per D-03 and D-04:
1. Create `src/proxy.test.ts` with the same content as `src/lib/security-headers.test.ts`
2. Change the import on line 2 from `import proxy, { config } from '../../src/proxy'` to `import proxy, { config } from './proxy'`
3. Delete `src/lib/security-headers.test.ts`

The rest of the test file remains unchanged -- same describe block, same 6 test cases, same assertions.

After relocation, run `npx vitest run src/proxy.test.ts` to confirm the relocated test passes.
  </action>
  <verify>
    <automated>npx vitest run src/proxy.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/security-headers.test.ts` does not exist
    - `src/proxy.test.ts` exists
    - `grep "from './proxy'" src/proxy.test.ts` matches (correct relative import)
    - `grep -v "../../src/proxy" src/proxy.test.ts` confirms old import path is gone (no match for old path)
    - `npx vitest run src/proxy.test.ts` passes all 6 tests
  </acceptance_criteria>
  <done>security-headers.test.ts relocated to src/proxy.test.ts with corrected import path. All 6 tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Fix tsconfig types and error.test.tsx shadowing (HYGN-04)</name>
  <files>tsconfig.json, src/app/error.test.tsx</files>
  <read_first>
    - tsconfig.json
    - src/app/error.test.tsx
    - src/app/error.tsx
  </read_first>
  <action>
Two changes to eliminate all 10 `tsc --noEmit` errors:

**A. tsconfig.json (per D-05):**
Add `"types": ["vitest/globals"]` to the `compilerOptions` object. Place it after `"jsx": "react-jsx"`. The full compilerOptions block should read:

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "types": ["vitest/globals"],
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": {
      "@/*": ["./src/*"],
      "@/.velite": ["./.velite"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts", ".next/dev/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

This fixes the 2 errors about `afterEach` not found in use-glow-positions.test.ts and use-hero-animation.test.ts. Do NOT add `@testing-library/jest-dom` to the types array -- it is already handled by vitest.setup.ts importing `@testing-library/jest-dom/vitest`.

**B. src/app/error.test.tsx (per D-06):**
Rename the default import from `Error` to `ErrorPage` to stop shadowing the global `Error` constructor. Change line 3 from:
```typescript
import Error from './error'
```
to:
```typescript
import ErrorPage from './error'
```

Then update all 4 JSX render calls from `<Error` to `<ErrorPage`:
- Line 7: `render(<ErrorPage error={new Error('boom')} reset={() => {}} />)`
- Line 12: `render(<ErrorPage error={new Error('boom')} reset={() => {}} />)`
- Line 19: `render(<ErrorPage error={new Error('boom')} reset={reset} />)`
- Line 25: `render(<ErrorPage error={new Error('boom')} reset={() => {}} />)`

This fixes the 8 errors (TS7009 + TS2345 on each of the 4 `new Error()` calls) caused by `Error` referring to the React component instead of the global Error constructor.

After both changes, run:
1. `npm run test -- --run` to confirm all 132 tests still pass
2. `npx tsc --noEmit` to confirm zero errors
  </action>
  <verify>
    <automated>npm run test -- --run 2>&1 | tail -3 && echo "---TSC---" && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"types"' tsconfig.json` shows `"types": ["vitest/globals"]`
    - `grep "import ErrorPage from './error'" src/app/error.test.tsx` matches
    - `grep "import Error from" src/app/error.test.tsx` does NOT match (old import gone)
    - `grep "<ErrorPage" src/app/error.test.tsx | wc -l` returns 4
    - `npm run test -- --run` passes with 132 tests
    - `npx tsc --noEmit` exits with code 0 and produces no error output
  </acceptance_criteria>
  <done>tsconfig.json has vitest/globals in types. error.test.tsx import renamed to ErrorPage. tsc --noEmit reports 0 errors. All 132 tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This phase is internal cleanup -- no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | tsconfig.json types array | accept | Adding vitest/globals to types is additive; skipLibCheck:true prevents type-resolution regressions. Verified in research that no @types/* packages are excluded by this change. |
| T-20-02 | Information Disclosure | Test file relocation | accept | Test files contain no secrets; relocation is purely organizational. |
</threat_model>

<verification>
Phase-level verification after all tasks complete:

1. `ls src/components/blog/copy-button.tsx src/components/blog/copy-button.test.tsx 2>&1` -- both report "No such file"
2. `ls src/lib/security-headers.test.ts 2>&1` -- reports "No such file"
3. `ls src/proxy.test.ts` -- file exists
4. `grep lucide-react package.json` -- dependency present
5. `npm run test -- --run` -- 132 tests pass, 0 failures
6. `npx tsc --noEmit` -- 0 errors
7. `npm run lint` -- 0 errors
</verification>

<success_criteria>
1. CopyButton component and its test file are deleted; `npm run test` passes with exactly 132 tests
2. lucide-react remains in package.json (6 other consumers exist)
3. security-headers.test.ts has been relocated to src/proxy.test.ts with corrected import path and passes
4. `npx tsc --noEmit` reports zero errors across the entire codebase including test files
</success_criteria>

<output>
After completion, create `.planning/phases/20-dead-code-test-hygiene/20-01-SUMMARY.md`
</output>

---
phase: quick
plan: 260403-h6t
type: execute
wave: 1
depends_on: []
files_modified:
  - eslint.config.mjs
  - package.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "npm run lint executes ESLint successfully without errors"
    - "No FlatCompat or @eslint/eslintrc usage remains"
    - ".velite/ directory is excluded from linting"
  artifacts:
    - path: "eslint.config.mjs"
      provides: "Native flat config using eslint-config-next direct imports"
    - path: "package.json"
      provides: "lint script using eslint directly instead of next lint"
  key_links: []
---

<objective>
Fix broken `npm run lint` by replacing the removed `next lint` command with direct `eslint` invocation and rewriting `eslint.config.mjs` to use native flat config imports instead of the broken `FlatCompat` wrapper.

Purpose: `npm run lint` has been broken since the Next.js 16 upgrade -- `next lint` was removed and `eslint-config-next` now exports native flat config that is incompatible with `FlatCompat`.
Output: Working lint command, clean ESLint config, `@eslint/eslintrc` removed from devDependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260403-h6t-broken-npm-run-lint-fixable-by-switching/260403-h6t-RESEARCH.md
@eslint.config.mjs
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ESLint config and lint script</name>
  <files>eslint.config.mjs, package.json</files>
  <action>
1. Rewrite `eslint.config.mjs` to use native flat config imports (no FlatCompat):

```js
import coreWebVitals from "eslint-config-next/core-web-vitals";
import typescript from "eslint-config-next/typescript";

export default [
  { ignores: [".velite/"] },
  ...coreWebVitals,
  ...typescript,
];
```

2. In `package.json`, change the lint script from `"next lint"` to `"eslint ."`.

3. Run `npm uninstall @eslint/eslintrc` to remove the now-unnecessary FlatCompat dependency.

4. Run `npm run lint` and fix any legitimate lint errors that surface (these would be pre-existing issues newly caught). If there are path alias resolution issues with `@/*` or `@/.velite`, verify `tsconfig.json` paths are correctly configured -- `eslint-config-next` includes `eslint-import-resolver-typescript` which should handle this automatically.
  </action>
  <verify>
    <automated>cd /home/smada/git/smadam813/keech-dev && npm run lint</automated>
  </verify>
  <done>npm run lint completes successfully, no circular structure errors, no "invalid directory" errors, .velite/ excluded from linting, @eslint/eslintrc removed from devDependencies</done>
</task>

</tasks>

<verification>
- `npm run lint` runs without errors
- `grep -c "FlatCompat" eslint.config.mjs` returns 0
- `grep -c "@eslint/eslintrc" package.json` returns 0
- `npm run build` still succeeds (lint config does not interfere with build)
</verification>

<success_criteria>
- ESLint runs successfully via `npm run lint`
- eslint.config.mjs uses native flat config (no FlatCompat wrapper)
- @eslint/eslintrc removed from devDependencies
- .velite/ directory ignored by ESLint
</success_criteria>

<output>
After completion, create `.planning/quick/260403-h6t-broken-npm-run-lint-fixable-by-switching/260403-h6t-SUMMARY.md`
</output>

---
phase: 21-dependency-upgrades
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - DEPS-01

must_haves:
  truths:
    - "tailwindcss and @tailwindcss/postcss are both at 4.2.x (matching versions)"
    - "tailwind-merge is at 3.5.x"
    - "@upstash/redis is at 1.37.x"
    - "rehype-pretty-code is at 0.14.3"
    - "@types/node is at 25.5.x and @types/react is at 19.2.14+"
    - "npm run build && npm run test && npm run lint all pass with zero errors"
    - "npm audit reports zero vulnerabilities"
  artifacts:
    - path: "package.json"
      provides: "Updated minor/patch dependency versions"
      contains: "tailwindcss"
    - path: "package-lock.json"
      provides: "Regenerated lock file"
  key_links: []
---

<objective>
Apply all minor/patch dependency updates as the first risk-tiered batch (per D-01).

Purpose: Get the low-risk version bumps out of the way with full validation before tackling major upgrades.
Output: Updated package.json and package-lock.json with all minor/patch deps at current stable versions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-dependency-upgrades/21-CONTEXT.md
@.planning/phases/21-dependency-upgrades/21-RESEARCH.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply minor/patch dependency updates</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current dependency versions)
    - .planning/phases/21-dependency-upgrades/21-RESEARCH.md (exact target versions and install commands)
  </read_first>
  <action>
Run the following npm install commands to update all minor/patch dependencies (per D-01, DEPS-01):

```bash
npm install tailwindcss@^4.2.2 @tailwindcss/postcss@^4.2.2 tailwind-merge@^3.5.0 @upstash/redis@^1.37.0 rehype-pretty-code@^0.14.3
npm install -D @types/node@^25.5.2 @types/react@^19.2.14
```

CRITICAL: tailwindcss and @tailwindcss/postcss MUST be upgraded together in the same command (per Pitfall 2 in RESEARCH.md -- version mismatch causes build failures).

After install, verify package.json has updated version ranges and package-lock.json has been regenerated.

Then run full pipeline validation (per D-06):
```bash
npm run build && npm run test && npm run lint && npm audit
```

All four commands must pass with zero errors and zero vulnerabilities.
  </action>
  <verify>
    <automated>npm run build && npm run test && npm run lint && npm audit</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"tailwindcss": "^4.2.2"`
    - package.json contains `"@tailwindcss/postcss": "^4.2.2"`
    - package.json contains `"tailwind-merge": "^3.5.0"`
    - package.json contains `"@upstash/redis": "^1.37.0"`
    - package.json contains `"rehype-pretty-code": "^0.14.3"`
    - package.json contains `"@types/node": "^25.5.2"`
    - package.json contains `"@types/react": "^19.2.14"`
    - `npm run build` exits 0
    - `npm run test` exits 0 with all tests passing
    - `npm run lint` exits 0 with zero errors
    - `npm audit` reports 0 vulnerabilities
  </acceptance_criteria>
  <done>All minor/patch dependencies updated to current stable versions. Full pipeline validation passes with zero errors and zero vulnerabilities.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> package-lock.json | New package versions fetched from npm registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | npm packages | mitigate | Run `npm audit` after install to verify no known vulnerabilities in updated packages |
</threat_model>

<verification>
- `npm run build && npm run test && npm run lint && npm audit` all exit 0
- package.json version ranges updated for all 7 packages
</verification>

<success_criteria>
All minor/patch dependencies are at their current stable versions. The build, test suite, linter, and audit all pass cleanly. No code changes required -- version bumps only.
</success_criteria>

<output>
After completion, create `.planning/phases/21-dependency-upgrades/21-01-SUMMARY.md`
</output>

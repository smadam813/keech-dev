---
phase: 01-foundation-design
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/layout/header.tsx
  - src/components/layout/mobile-nav.tsx
  - src/components/layout/footer.tsx
  - src/app/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Desktop users see fixed header at top of screen while scrolling"
    - "Mobile users see fixed bottom nav bar with icons"
    - "Navigation includes links to Home, Blog, Projects, About"
    - "Footer displays GitHub and LinkedIn social links"
    - "Navigation visually reinforces neobrutalist brand (borders, shadows)"
  artifacts:
    - path: "src/components/layout/header.tsx"
      provides: "Desktop navigation header"
      exports: ["Header"]
    - path: "src/components/layout/mobile-nav.tsx"
      provides: "Mobile bottom navigation"
      exports: ["MobileNav"]
    - path: "src/components/layout/footer.tsx"
      provides: "Footer with social links"
      exports: ["Footer"]
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/layout/header.tsx"
      via: "import and render"
      pattern: "import.*Header.*from.*components/layout"
    - from: "src/app/layout.tsx"
      to: "src/components/layout/mobile-nav.tsx"
      via: "import and render"
      pattern: "import.*MobileNav.*from.*components/layout"
---

<objective>
Create navigation shell: desktop header, mobile bottom nav, and footer with social links.

Purpose: Navigation is the spine of the site. Users need to move between sections, and the nav must reinforce the neobrutalist brand identity with bold borders and shadows.

Output: Complete navigation system that works on desktop (fixed top header) and mobile (fixed bottom nav bar), with footer containing social links.
</objective>

<execution_context>
@/home/smada/.claude/get-shit-done/workflows/execute-plan.md
@/home/smada/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-design/01-CONTEXT.md
@.planning/phases/01-foundation-design/01-RESEARCH.md
@.planning/phases/01-foundation-design/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Desktop Header and Mobile Nav</name>
  <files>
    src/components/layout/header.tsx
    src/components/layout/mobile-nav.tsx
  </files>
  <action>
Install lucide-react for icons: `npm install lucide-react`

Create src/components/layout/header.tsx (desktop fixed header):
```tsx
import Link from 'next/link'

const navItems = [
  { href: '/', label: 'Home' },
  { href: '/blog', label: 'Blog' },
  { href: '/projects', label: 'Projects' },
  { href: '/about', label: 'About' },
]

export function Header() {
  return (
    <header className="fixed top-0 left-0 right-0 z-50 bg-background border-b-[3px] border-foreground hidden md:block">
      <div className="max-w-5xl mx-auto px-6 h-16 flex items-center justify-between">
        <Link href="/" className="font-display font-bold text-2xl hover:text-accent transition-colors">
          keech.dev
        </Link>
        <nav className="flex gap-8">
          {navItems.map((item) => (
            <Link
              key={item.href}
              href={item.href}
              className="font-display font-medium text-lg hover:text-accent transition-colors"
            >
              {item.label}
            </Link>
          ))}
        </nav>
      </div>
    </header>
  )
}
```

Create src/components/layout/mobile-nav.tsx (bottom nav for mobile):
```tsx
'use client'

import Link from 'next/link'
import { usePathname } from 'next/navigation'
import { Home, FileText, Folder, User } from 'lucide-react'
import { cn } from '@/lib/utils'

const navItems = [
  { href: '/', icon: Home, label: 'Home' },
  { href: '/blog', icon: FileText, label: 'Blog' },
  { href: '/projects', icon: Folder, label: 'Projects' },
  { href: '/about', icon: User, label: 'About' },
]

export function MobileNav() {
  const pathname = usePathname()

  return (
    <nav
      className="fixed bottom-0 left-0 right-0 z-50 md:hidden bg-background border-t-[3px] border-foreground"
      style={{ paddingBottom: 'env(safe-area-inset-bottom)' }}
    >
      <div className="flex justify-around items-center h-16">
        {navItems.map((item) => {
          const isActive = pathname === item.href ||
            (item.href !== '/' && pathname.startsWith(item.href))
          const Icon = item.icon

          return (
            <Link
              key={item.href}
              href={item.href}
              className={cn(
                'flex flex-col items-center justify-center w-16 h-full',
                'transition-colors',
                isActive ? 'text-accent' : 'text-foreground'
              )}
            >
              <Icon className="w-6 h-6" strokeWidth={isActive ? 2.5 : 2} />
              <span className="text-xs mt-1 font-medium">{item.label}</span>
            </Link>
          )
        })}
      </div>
    </nav>
  )
}
```

Key implementation details:
- Header hidden on mobile (hidden md:block), visible on md+ screens
- Mobile nav hidden on desktop (md:hidden), visible on mobile
- Both use neobrutalist 3px borders (border-[3px] border-foreground)
- Mobile nav uses safe-area-inset for iPhone home bar
- Active state uses accent color for visual feedback
  </action>
  <verify>
    `npm run build` completes without TypeScript errors
    At desktop width (>768px): header visible at top, bottom nav hidden
    At mobile width (<768px): header hidden, bottom nav visible at bottom
    All 4 nav links render (Home, Blog, Projects, About)
  </verify>
  <done>
    Desktop header component created with fixed positioning and brand logo
    Mobile bottom nav component created with icons and active states
    Both components use neobrutalist styling (thick borders, accent colors)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Footer and Wire Layout</name>
  <files>
    src/components/layout/footer.tsx
    src/app/layout.tsx
  </files>
  <action>
Create src/components/layout/footer.tsx:
```tsx
import Link from 'next/link'
import { Github, Linkedin } from 'lucide-react'

const socialLinks = [
  { href: 'https://github.com/smadam813', icon: Github, label: 'GitHub' },
  { href: 'https://linkedin.com/in/smadam813', icon: Linkedin, label: 'LinkedIn' },
]

export function Footer() {
  return (
    <footer className="bg-foreground text-background py-8 mt-auto">
      <div className="max-w-5xl mx-auto px-6 flex flex-col md:flex-row items-center justify-between gap-4">
        <p className="font-display text-sm">
          {new Date().getFullYear()} keech.dev
        </p>
        <div className="flex gap-6">
          {socialLinks.map((link) => {
            const Icon = link.icon
            return (
              <Link
                key={link.href}
                href={link.href}
                target="_blank"
                rel="noopener noreferrer"
                className="hover:text-accent transition-colors"
                aria-label={link.label}
              >
                <Icon className="w-6 h-6" />
              </Link>
            )
          })}
        </div>
      </div>
    </footer>
  )
}
```

Update src/app/layout.tsx to include all navigation components:
```tsx
import type { Metadata } from "next";
import { spaceGrotesk, inter } from "@/lib/fonts";
import { Header } from "@/components/layout/header";
import { MobileNav } from "@/components/layout/mobile-nav";
import { Footer } from "@/components/layout/footer";
import "./globals.css";

export const metadata: Metadata = {
  title: "keech.dev",
  description: "Personal portfolio and blog",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${spaceGrotesk.variable} ${inter.variable} min-h-screen flex flex-col`}>
        <Header />
        <main className="flex-1 pt-0 md:pt-16 pb-20 md:pb-0">
          {children}
        </main>
        <Footer />
        <MobileNav />
      </body>
    </html>
  );
}
```

Layout structure explained:
- flex flex-col min-h-screen: Full viewport height with flex layout
- pt-0 md:pt-16: No top padding on mobile (no header), 16 (4rem) on desktop for fixed header
- pb-20 md:pb-0: Bottom padding on mobile for fixed nav, none on desktop
- Footer uses mt-auto to push to bottom when content is short
- MobileNav renders last (positioned fixed, order doesn't matter visually)

Note: Update social link URLs to match user's actual GitHub/LinkedIn handles if known.
  </action>
  <verify>
    `npm run build` completes without errors
    `npm run dev` - visit http://localhost:3000:
    - Desktop: Fixed header at top, footer at bottom, no overlap with content
    - Mobile: Fixed bottom nav, footer scrolls with content, no overlap
    - Footer shows GitHub and LinkedIn icons
    - Clicking nav links (will 404 for now, that's expected)
  </verify>
  <done>
    Footer component created with social links (GitHub, LinkedIn)
    Layout.tsx updated to render Header, MobileNav, and Footer
    Content area has proper padding to avoid overlap with fixed nav elements
    Navigation shell complete and ready for pages
  </done>
</task>

</tasks>

<verification>
Run full verification after all tasks:

1. `npm run build` - Builds without errors
2. `npm run dev` - Start dev server
3. Desktop verification (>768px viewport):
   - Fixed header visible at top with "keech.dev" logo and nav links
   - Header has thick bottom border (neobrutalist style)
   - Bottom nav is NOT visible
   - Footer is visible at bottom with social icons
4. Mobile verification (<768px viewport):
   - Header is NOT visible
   - Fixed bottom nav visible with 4 icons
   - Bottom nav has thick top border
   - Footer scrolls with content (not fixed)
5. Interactive check:
   - Nav links are clickable (will 404 until pages created)
   - Social links open in new tabs
   - Active state shows accent color on mobile nav
</verification>

<success_criteria>
- Desktop: Fixed header at top, hidden on mobile
- Mobile: Fixed bottom nav at bottom, hidden on desktop
- All 4 navigation links present: Home, Blog, Projects, About
- Footer with GitHub and LinkedIn social links
- Neobrutalist styling: 3px borders, no gradients, bold visual presence
- No content overlap with fixed navigation elements
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design/01-02-SUMMARY.md`
</output>

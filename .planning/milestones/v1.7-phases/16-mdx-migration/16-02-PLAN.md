---
phase: 16-mdx-migration
plan: "02"
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/proxy.ts
autonomous: false
requirements: [MDX-05]

must_haves:
  truths:
    - "Browser console shows no CSP violations on any page"
    - "unsafe-eval is absent from the Content-Security-Policy header"
    - "All existing pages still render correctly after CSP tightening"
  artifacts:
    - path: "src/proxy.ts"
      provides: "CSP without unsafe-eval in script-src"
      contains: "script-src 'self' 'unsafe-inline'"
  key_links:
    - from: "src/proxy.ts"
      to: "browser CSP enforcement"
      via: "Content-Security-Policy header"
      pattern: "script-src 'self' 'unsafe-inline'"
---

<objective>
Remove unsafe-eval from CSP script-src directive now that MDXContent no longer uses new Function().

Purpose: This is the security payoff of the entire MDX migration -- hardening CSP by eliminating eval-based code execution.
Output: Tightened CSP in proxy.ts, verified via e2e tests and visual checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-mdx-migration/16-CONTEXT.md
@.planning/phases/16-mdx-migration/16-01-SUMMARY.md

@src/proxy.ts

<interfaces>
From src/proxy.ts (current CSP line):
```typescript
"script-src 'self' 'unsafe-eval' 'unsafe-inline' https://va.vercel-scripts.com",
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unsafe-eval from CSP and run e2e tests</name>
  <files>src/proxy.ts</files>
  <read_first>
    - src/proxy.ts (current middleware with CSP directives)
    - src/components/blog/mdx-content.tsx (confirm new Function is gone from Plan 01)
  </read_first>
  <action>
    Per D-08, modify src/proxy.ts to remove `'unsafe-eval'` from the script-src directive.

    Change line:
    ```typescript
    "script-src 'self' 'unsafe-eval' 'unsafe-inline' https://va.vercel-scripts.com",
    ```

    To:
    ```typescript
    "script-src 'self' 'unsafe-inline' https://va.vercel-scripts.com",
    ```

    This is a single string edit -- remove ` 'unsafe-eval'` (including the leading space) from the script-src directive in the cspDirectives array.

    Then run the full e2e test suite to verify no CSP violations break functionality:
    ```bash
    npm run build && npx playwright test
    ```
  </action>
  <verify>
    <automated>npm run build && npx playwright test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/proxy.ts contains `"script-src 'self' 'unsafe-inline' https://va.vercel-scripts.com"`
    - src/proxy.ts does NOT contain `unsafe-eval`
    - `npm run build` exits 0
    - `npx playwright test` exits 0 (all e2e tests pass including code-copy.spec.ts)
  </acceptance_criteria>
  <done>unsafe-eval removed from CSP, all e2e tests pass confirming no CSP violations.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of blog rendering and copy button</name>
  <files>none</files>
  <action>
    Human verifies the complete MDX migration visually. No code changes -- this is a verification checkpoint.
  </action>
  <what-built>
    Complete MDX migration: blog posts now render from compiled HTML instead of runtime JavaScript execution. Code block copy buttons are injected via DOM manipulation. Lists have role="list" for accessibility. CSP no longer allows unsafe-eval.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000/blog
    2. Click into a blog post that has code blocks
    3. Verify: Post content renders correctly (headings, paragraphs, lists, code blocks all visible)
    4. Verify: Code blocks have syntax highlighting (colored tokens, dark background)
    5. Hover over a code block -- verify the copy button appears (top-right corner)
    6. Click the copy button -- verify it shows "Copied!" state and clipboard has the code text
    7. Open browser DevTools > Console -- verify NO CSP violation errors
    8. Open browser DevTools > Network -- click a page response > Headers -- verify Content-Security-Policy does NOT contain unsafe-eval
    9. Check a post with bullet lists -- verify they render as bullets (not unstyled)
  </how-to-verify>
  <verify>User confirms all 9 verification steps pass</verify>
  <done>User approves visual and functional correctness of the MDX migration.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `grep 'unsafe-eval' src/proxy.ts` returns no matches
2. `npm run test:e2e` -- all e2e tests pass (code-copy, mobile menu, view counts, mobile TOC)
3. `npm run build` -- all pages remain static
4. Browser console shows zero CSP violations
</verification>

<success_criteria>
- unsafe-eval removed from CSP script-src
- All e2e tests pass
- Blog posts render correctly with copy buttons and syntax highlighting
- No CSP violations in browser console
</success_criteria>

<output>
After completion, create `.planning/phases/16-mdx-migration/16-02-SUMMARY.md`
</output>

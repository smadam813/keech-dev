{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path'); case \"$f\" in *.ts|*.tsx) out=$(NO_COLOR=1 npx eslint --fix \"$f\" 2>&1) || { echo \"$out\" >&2; exit 2; };; esac",
            "timeout": 60,
            "statusMessage": "ESLint on edited file"
          }
        ]
      }
    ]
  }
}

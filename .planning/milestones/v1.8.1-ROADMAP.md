# Roadmap Archive: v1.8.1 Address Missed Concerns

**Archived:** 2026-04-11
**Status:** SHIPPED
**PR:** #32 (squash-merged to preview)

For current roadmap, see `.planning/ROADMAP.md`.

---

## v1.8.1 Address Missed Concerns

**Goal:** Close the three CONCERNS.md items that were out of scope during v1.8 so the audit backlog is fully resolved.

**Timeline:** 2026-04-11 (single day)
**Phases:** 1 | **Plans:** 3 | **Commits:** 27 | **Source changes:** 12 files, +126/-38 lines

### Phase 24: Audit Gap Closures
**Goal**: Close the three CONCERNS.md items that were out of scope during v1.8 so the audit backlog is fully resolved
**Requirements**: GAP-01, GAP-02, GAP-03
**Success Criteria**:
  1. Visiting `/blog/<slug>` for a post with `draft: true` returns 404
  2. `next build` output does not include any draft-slug routes
  3. `npm run test:e2e:dev` runs Playwright against dev server
  4. Clipboard copy button surfaces visible failure state on rejection
  5. Unit tests cover the clipboard failure path

Plans:
- [x] 24-01-draft-guard — Extract publishedPosts helper and wire through all 5 call sites (GAP-01)
- [x] 24-02-dev-e2e-script — Add env-var ternary to Playwright config and test:e2e:dev script (GAP-02)
- [x] 24-03-clipboard-failure — Add try/catch + xIcon + aria-live to clipboard handler and failure test (GAP-03)

### Key Accomplishments

1. **Draft post leak prevention** — `publishedPosts` helper as single source of truth across all 5 call sites (sitemap, feed.xml, blog listing, slug page, OG image), with `notFound()` guard on OG route
2. **Dev-server E2E script** — `test:e2e:dev` runs Playwright against Turbopack dev server (~5s startup vs ~60s full build)
3. **Clipboard failure handling** — visible X icon + 'Copy failed' aria-label + console.error + aria-live announcements for screen readers
4. **Test coverage** — 157 unit tests (up from 154), UAT 7/7 passed, lint clean

### Verification

- 5/5 automated must-haves verified
- 2 human verification items noted (draft slug 404, dev E2E full run)
- UAT: 7 passed, 0 issues

---
phase: 03-projects-about
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [src/app/globals.css]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Inline code renders inline within paragraph flow"
    - "Code blocks retain neobrutalist block styling"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Differentiated selectors for inline vs block code"
      contains: "figure[data-rehype-pretty-code-figure]"
  key_links:
    - from: "globals.css block styles"
      to: "figure elements only"
      via: "element-qualified selector"
      pattern: "figure\\[data-rehype-pretty-code-figure\\]"
---

<objective>
Fix inline code styling so it renders inline within paragraphs instead of as block elements.

Purpose: rehype-pretty-code applies the same `data-rehype-pretty-code-figure` attribute to both code blocks (on `<figure>`) and inline code (on `<span>`). Current CSS applies block-level styling to both. This fix differentiates the selectors.

Output: Inline code like `@theme` flows naturally within text; code blocks retain neobrutalist styling.
</objective>

<execution_context>
@/home/smada/.claude/get-shit-done/workflows/execute-plan.md
@/home/smada/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/debug/inline-code-block-styling.md
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Differentiate inline and block code CSS selectors</name>
  <files>src/app/globals.css</files>
  <action>
Update the code block CSS selectors to target only block-level code:

1. Change `[data-rehype-pretty-code-figure]` (line 45) to `figure[data-rehype-pretty-code-figure]`
   - This ensures block styling (my-6, border, shadow, rounded-lg) only applies to figure elements

2. Change `[data-rehype-pretty-code-figure] pre` (line 60) to `figure[data-rehype-pretty-code-figure] pre`
   - Keeps overflow and padding scoped to block code

3. Change `[data-rehype-pretty-code-figure] code` (line 66) to `figure[data-rehype-pretty-code-figure] code`
   - Keeps display:grid scoped to block code only

4. Add inline code styling for rehype-pretty-code processed inline code after the existing inline code rule (after line 106):
```css
/* Inline code processed by rehype-pretty-code */
span[data-rehype-pretty-code-figure] code {
  @apply px-1.5 py-0.5 rounded font-mono text-sm;
  background-color: var(--color-surface);
  border: 1px solid var(--color-foreground);
}
```

This ensures inline code gets the same visual treatment as the existing `:not(pre) > code` rule.
  </action>
  <verify>
1. Run `npm run build` - should complete without errors
2. Run `npm run dev` and visit /projects/keech-dev
3. Verify inline code like `@theme` renders inline within paragraph text
4. Verify code blocks still have neobrutalist styling (border, shadow, rounded corners)
  </verify>
  <done>
Inline code flows naturally within paragraphs. Code blocks retain full neobrutalist styling with borders, shadows, and rounded corners.
  </done>
</task>

</tasks>

<verification>
- [ ] `npm run build` passes
- [ ] /projects/keech-dev page renders without errors
- [ ] Inline code displays inline within text (not as block element)
- [ ] Code blocks retain neobrutalist styling (border, shadow, language badge)
</verification>

<success_criteria>
1. Inline code in MDX renders as inline styled element within paragraph flow
2. Code blocks unchanged - still display with neobrutalist borders and shadows
3. No regressions to blog post code blocks at /blog/hello-world
</success_criteria>

<output>
After completion, create `.planning/phases/03-projects-about/03-04-SUMMARY.md`
</output>
